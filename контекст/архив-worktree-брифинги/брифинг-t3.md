# Брифинг — Блок C: T3 Гант (worktree feat/t3-gantt)

> Этот чат работает ТОЛЬКО над T3. Параллельно идут T2 (feat/t2-calendar) и T4 (feat/t4-goals).
> Worktree: `Alexandr/planner-wt-t3`. Ветка `feat/t3-gantt` от `d934e7b`.

## Сначала прочитай
- `CLAUDE.md` (хаб planner) + `planner-v2/.claude/TEAM.md`
- `memory/project-state.md` — где встали
- `контекст/конвейер-плейбук.md` — рецепт + гейт + деплой

## Задача
Стаб-таб «Гант» → полноценный экран. Мокапы `база-проекта-v3/pages/` (T3a/T3b). Эталон процесса = T1.

## Бэк — НУЖНЫ drag-эндпоинты (НЕ готов)
Гант = перенос/растяжка полос задач по датам. Нужны эндпоинты обновления due_date/end_date задачи (end_date УЖЕ есть в модели — добавлен этой сессией, миграция b9c0d1e2f3a4). Проверь `src/planner/api/routes/tasks.py` PATCH (поддерживает due_date/end_date/due_time/end_time). Возможно хватит существующего PATCH — тогда нового бэка нет. Если нужен спец-эндпоинт (batch/range) — спроектируй с TDD (pytest), бэк-правила ZERO-AFK (app не зовёт LLM).

## ВАЖНО — зависимость
`end_date` (многодневный спан) только что добавлен. Гант опирается на него. Твоя ветка от d934e7b → end_date уже в коде. ОК.

## Твои файлы
`frontend/src/screens/Gantt*.tsx` (создать), preview-t3, `tests-e2e/T3-*.spec.ts`. Бэк — если нужен: `routes/tasks.py`/`schemas.py` (ОСТОРОЖНО — общий с другими, дописывай).

## Общие — ТОЛЬКО ДОПИСЫВАЙ (theme.css `gantt-*`, types.ts, api.ts, schemas.py). Нав не трогай.

## Гейт + Деплой ПО ОЧЕРЕДИ
Гейт T1 (lint:tokens/test/e2e/tsc + pytest если трогал бэк + coverage + визуал). НЕ деплой без сигнала (прод один). Готов → коммит → merge+serial deploy.

## Правила: TDD, depth-first, мокап=контракт, verify перед деплоем, emoji только иконка.
