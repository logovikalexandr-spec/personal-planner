# Брифинг — Блок B: T4 Цели (worktree feat/t4-goals)

> Этот чат работает ТОЛЬКО над T4. Параллельно идут T2 (feat/t2-calendar) и T3 (feat/t3-gantt).
> Worktree: `Alexandr/planner-wt-t4`. Ветка `feat/t4-goals` от `d934e7b`.

## Сначала прочитай
- `CLAUDE.md` (хаб planner) + `planner-v2/.claude/TEAM.md`
- `memory/project-state.md` — там блок про T4: НАЧАТ, WIP сохранён в коммите `35a10f1`
- `контекст/конвейер-плейбук.md` — рецепт + гейт + деплой

## Задача
Заглушка `Goals.tsx` → полноценный экран Цели. Мокапы `база-проекта-v3/pages/T4-celi.html` (список) + `T4d` (детали). Эталон процесса = T1.

## WIP УЖЕ В ТВОЕЙ ВЕТКЕ (коммит 35a10f1 — предок d934e7b, проверено)
Фронт Форк0 уже в коде, ничего доставать не надо: `types.ts` (Project +success_probability/target_date/ai_notes/weeks_left, тип AiNote), `api.ts` (getStages, updateProjectAI — write-API ZERO-AFK), `Goals.tsx` (список A1-A8: пульс/кольцо%/полоски этапов/веха/AI-заметка, derive пульс/статус client-side). **Бери `screens/Goals.tsx` как есть за основу.**

## Бэк — ГОТОВ (Форк0)
stages + updateProjectAI. Доп-пробелы для деталей T4d (недельная дельта %, структурный разбор) — потом, список не требует.

## Осталось (по плейбуку)
T4-стили в `theme.css` (классы `t4-pulse/t4-card/t4-ring/t4-stages/t4-ai/t4-newbtn`, project.color через `--c`) → `preview-t4-mock` (stateful) → визуал-сверка vs `T4-celi.html` → e2e `tests-e2e/T4-celi.spec.ts` (эталон T1) → coverage → деплой.

## Твои файлы
`screens/Goals.tsx`, preview-t4, `tests-e2e/T4-*.spec.ts`.

## Общие — ТОЛЬКО ДОПИСЫВАЙ (theme.css `t4-*`, types.ts, api.ts). Нав не трогай.

## Гейт + Деплой ПО ОЧЕРЕДИ
Гейт как у T1. НЕ деплой без сигнала координатора (прод один). Готов → коммит → merge+serial deploy.

## Правила: TDD, depth-first, мокап=контракт, verify перед деплоем, emoji только иконка.
