# Брифинг — Блок A: T2 Календарь (worktree feat/t2-calendar)

> Этот чат работает ТОЛЬКО над T2. Параллельно идут T4 (feat/t4-goals) и T3 (feat/t3-gantt).
> Worktree: `Alexandr/planner-wt-t2`. Ветка `feat/t2-calendar` от `d934e7b`.

## Сначала прочитай
- `CLAUDE.md` (хаб planner) + `planner-v2/.claude/TEAM.md`
- `memory/project-state.md` (верхний блок — где встали)
- `контекст/конвейер-плейбук.md` — рецепт «добить экран» + гейт + деплой

## Задача
Календарь: виды **Неделя / Месяц / Лента** (сейчас только вид Дня). Мокапы — найди в `база-проекта-v3/pages/` (T2-*). Эталон процесса = T1 (добит на 100%).

## Бэк — ГОТОВ (Форк B)
range-запрос задач + `GET /api/milestones`. Проверь в `src/planner/api/routes/`. Новый бэк не нужен (если нужен — флагни).

## Твои файлы (свобода)
- `frontend/src/screens/Calendar.tsx` (главный)
- `frontend/src/lib/weekLayout.ts` (+ .test) — хелперы недели уже есть
- preview-моки T2 + e2e `tests-e2e/T2-*.spec.ts`

## Общие файлы — ТОЛЬКО ДОПИСЫВАЙ свою секцию (не переписывай!)
`theme.css` (классы `cal-week-*`/`cal-month-*`), `types.ts`, `api.ts`, `App.tsx` (нав уже на 5 табов — не трогай нав). Конфликты при merge будут аддитивные.

## Гейт (перед «готово»)
`cd planner-v2/frontend && npm run lint:tokens && npm run test && npm run e2e && npx tsc --noEmit` + coverage по плейбуку + визуал-сверка мокап↔рендер.

## Деплой — ПО ОЧЕРЕДИ (прод один!)
НЕ деплой сам без сигнала. Готов → коммит в ветку, скажи координатору (главный чат) → merge + serial deploy. Два build+up одновременно = поломка.

## Правила
TDD (тест до кода), depth-first (состояния+edge), мокап=контракт, verify перед деплоем. emoji только иконка проекта.
