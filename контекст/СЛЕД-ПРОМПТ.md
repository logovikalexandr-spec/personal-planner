# Стартовый промпт для новой сессии (вставить целиком)

---

Работаем над planner. Прочитай `personal-planner/memory/project-state.md` (верхний блок 2026-06-14 вечер) и хэндофф `personal-planner/контекст/сессии/2026-06-14-3.md`.

Creation-flow задачи доведён и на проде — не трогаем. На эту сессию ДВЕ задачи (обе код, не вслепую — verify перед деплоем):

1. **Переделать «Длительность» → пикер «Дата/время»** (TickTick старт→дедлайн). Дизайн уже утверждён. Читай спеку `planner-v2/docs/superpowers/specs/2026-06-14-datetime-picker-rework-design.md` + мокап `база-проекта-v3/pages/T1-datetime.html`. Переписать `DateSheet.tsx` + бэк `Task.end_date` (миграция). Гейт + e2e-verify перед деплоем.

2. **Баг drag наложенных блоков:** блок в колонке 1 (наложение по времени) не двигается long-press'ом, в колонке 0 — двигается (resize у обоих работает). Построй репро-харнес с 2 наложенными блоками, воспроизведи, найди корень в `DayTimeline.tsx` (`onBodyDown`/`onDragMove`), почини. НЕ блайнд-фикс.

С какой начнём? Предложи план по первой.

---
