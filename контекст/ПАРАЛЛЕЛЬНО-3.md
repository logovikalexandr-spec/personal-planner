# 3 блока для параллельной работы (разные чаты)

> Прод жив, creation-flow готов. Каждый блок = отдельный чат. Файлы почти не пересекаются.
> Общий контекст для всех: `memory/project-state.md` (верхний блок) + `контекст/сессии/2026-06-14-3.md`.

## ПРАВИЛА ПАРАЛЛЕЛИ (для всех 3)
- **git worktree-изоляция** на блок (урок гонки общих файлов): `git worktree add ../pp-<блок> planner-v2-phase0`. Работать в своей копии.
- **НЕ вслепую:** verify (e2e/repro) ПЕРЕД деплоем. Гейт зелёный: `npm run lint:tokens && npm run test && npm run e2e && npx tsc --noEmit`.
- **Деплой — по очереди** (прод один, `/root/planner-v2-src`). Не деплоить одновременно. Кто домержил — тот гонит rsync+build+up, остальные ребейзятся.
- **Зоны ответственности (чтоб не толкаться):**
  - theme.css → только Блок 3 (T4). Блоки 1/2 переиспользуют классы, новых стилей в theme.css не пишут (если надо — в свой компонент scoped/инлайн, согласовать).
  - types.ts / models.py / schemas.py → только Блок 1 (datetime). Блоки 2/3 их не трогают.
  - DayTimeline.tsx → только Блок 2. DateSheet.tsx → только Блок 1. Goals.tsx → только Блок 3.

---

## БЛОК 1 — Пикер «Дата/время» (переделка «Длительность»)
**Цель:** убрать вкладки Дата/Длительность → единый пикер старт(дата+время)→дедлайн(дата+время) + heat-календарь + весь-день + длительность-метка.
**Читать:** спека `planner-v2/docs/superpowers/specs/2026-06-14-datetime-picker-rework-design.md` + мокап `база-проекта-v3/pages/T1-datetime.html`.
**Файлы:** `DateSheet.tsx` (переписать UI), бэк `models.py`/`schemas.py`/`routes/tasks.py` (+`Task.end_date`, миграция add-column), `types.ts` (DateValue+end_date — ЧАСТИЧНО уже есть). NB: `api.ts`/`TaskComposer.tsx`/`QuickAddBar.tsx` уже знают `end_date` — свериться, не дублировать.
**Скоуп:** один день = блок (как есть); многодневный = чип в all-day (полные спан-бары = follow-up, НЕ сейчас).
**Гейт:** vitest (длительность-метка, выбор начало/дедлайн) + pytest (round-trip end_date, null-совместимость) + e2e (открытие, тап дня→начало, 2-й день→дедлайн+спан, время-пилюля, Готово) + lint/tsc.

## БЛОК 2 — Баг drag наложенных блоков
**Цель:** блок в колонке наложения (col 1) не двигается long-press'ом, в col 0 — двигается (resize у обоих ок). Найти корень, починить.
**Читать:** `контекст/сессии/2026-06-14-3.md` (HANDOFF задача 2).
**Файлы:** только `DayTimeline.tsx` (+`lib/timelineLayout.ts` если корень там) + новый repro-preview/spec.
**Метод:** построить мини-харнес (preview с 2 наложенными timed-блоками 10:00–11:00 и 10:00–12:45) → воспроизвести long-press-move на col-1 → root-cause (подозрение: гонка pointer-capture/ре-рендер; `onBodyDown`/`onDragMove`) → фикс → e2e-доказательство ПЕРЕД деплоем. НЕ блайнд.
**Гейт:** e2e репро (move col-1 двигает блок) + 35 vitest + tsc.

## БЛОК 3 — T4 «Цели» (достроить WIP → мокап → залок → деплой)
**Цель:** довести начатый T4-список до мокапа, залочить тестами, задеплоить.
**Читать:** WIP-коммит `35a10f1` (Goals.tsx уже = список целей, фронт Форк0 проведён). Мокап `база-проекта-v3/pages/T4-celi.html` (ecodes A1-A8) + плейбук `контекст/конвейер-плейбук.md`.
**Файлы:** `Goals.tsx` (доводка), `theme.css` (T4-классы: t4-pulse/t4-card/t4-ring/t4-stages/t4-ai/t4-newbtn), `preview-t4-mock.tsx`+html, `tests-e2e/T4-celi.spec.ts`, coverage.
**Скоуп:** список целей (пульс A1 + карточки A2-A7 + новый A8). Деталь T4d = follow-up. Бэк Форк0 готов (getProjects+AI-поля, getStages).
**Гейт (плейбук):** preview → визуал-сверка с мокапом → data-testid → спека (эталон T1-*.spec.ts) → coverage PASS → lint/test/e2e/tsc → baseline (владелец) → деплой.
