# Сессия 2026-06-21 — трек ЗАДАЧИ: таймлайн-полировка + PWA + редизайн списков

> Worktree `planner-wt-tasks` / ветка `feat/tasks` → мёрдж в `planner-v2-phase0` → деплой.
> Параллельный чат Привычек в эту сессию ЗАВЕРШЁН (конфликтов нет). Прод итог: бандл `index-BT6McaRZ.js`.

## Что сделано (хронология, всё на проде)
1. **Скролл по карточке таймлайна** (Apple): onTS не бейлит на `.cal-block`; pd только на пустой сетке.
2. **Высота часа 56→96px** + **короткие встык-задачи в колонки** (`layoutColumns` visEnd = max(end, start+MIN_BLOCK_MIN)).
3. **Кант приоритета** — откатил capsule, оставил border-left (владельцу нравился); **цвет приоритета ЕДИНЫЙ** = `lib/projectColor` (high=danger/medium=accent/low=blue); текст короткого блока по центру.
4. **Инерция скролла velocity-gated** (флик катится, медленный драг встаёт); убраны `:active`-реакции карточки/ячейки при свайпе.
5. **Баг «скроллит страницу вместо таймлайна»** — `.daytimeline--static .cal-block/.cal-cb { touch-action:none }`.
6. **all-day тоггл** под «весь день» (⌄N/⌃) + reflow таймлайна; «весь день» в порядке смарт-списков.
7. **PWA «на экран Домой»** (вечер-8.5): vite-plugin-pwa+manifest+иконки+safe-area + вход device-токеном (бэк Bearer==PWA_TOKEN, бот `/applink`, TokenGate). → [[planner-pwa-install]].
8. **Шторка-баг** (вечер-8): scroll-lock body{position:fixed} ломал fixed-оверлеи в iOS standalone (чёрный провал) → для оверлеев без инпута лёгкий overflow:hidden.
9. **client-date** для смарт-списков (вечер-9): «Сегодня» по дате устройства, не сервера-UTC.
10. **Просрочка отдельным списком** + секции «Выполнено»/«Отменено» внизу (7 дней по done_at) (вечер-9). Мокап `design/tabs/mock2.html`.
11. **confirm-модал** (вечер-10): кастомный тёмный диалог в PWA вместо window.confirm; все удаления (задача/проект/привычка/метрика/замер) через `lib/confirm.confirmDialog`.

## Грабли сессии (в [[learnings]] + harness)
- iOS PWA: `tg()` НЕ undefined (SDK загружен) → Telegram-only гейтить по `isTelegram()`/initData, не по наличию метода.
- iOS PWA standalone: body{position:fixed} ломает fixed-оверлеи → не использовать как scroll-lock.
- SW-кэш PWA переживает удаление иконки → чистить через Данные сайтов; тестировать прод-PWA = сначала сброс SW.
- **НЕ автокликать delete на ПРОДЕ** (headless авто-принимает confirm; снёс задачу id73, восстановил id79). deleteTask=HARD.
- Деплой бэк-правок: rsync и `frontend/` и `src/`.

## ── HANDOFF (следующий чат) ──
- **Всё задеплоено**, git чист, всё в phase0. Прод бандл `index-BT6McaRZ.js`, health 200.
- **Главный фоллоу:** SW-кэш мешает — каждый деплой владелец чистит вручную. ПРЕДЛОЖЕНО авто-обновление PWA (баннер «Обновить» при новой версии SW / network-first шелл). Спросить владельца — делать?
- Device-проверка владельцем после сброса SW: confirm-модал на удалении, секции Выполнено/Отменено, Просрочено в шторке.
- Бэклог: T4 Цели, T3 Гант не построены. Красная подсветка «Просрочено» в шторке (опц.). PWA web-push напоминания (iOS 16.4+).
- Параллельная модель: трек=свой worktree+ветка, phase0=интеграция; перед деплоем feat→phase0, rsync frontend(+src если бэк); после — grep бандл-хэш+health.
