# Сессия 2026-06-12 — код-фаза: деплой ночь-3 + T1 добита до мокапа

## Что сделали
1. **Гейт снимка a0b245a+12 коммитов** прогнан: 153 pytest · 35 vitest · tsc 0 · vite build — зелёное.
2. **Деплой ночь-3 на прод** (`9ef9fd8`): impact + Форк E (привычки/метрики) + Tracking-проводка + Ретро + метка этапа A6.
   - Деплой = git push → ssh `/root/planner-v2-src` git pull --ff-only → `docker compose -f docker-compose.prod.yml up -d --build` (НЕ rsync — у planner-v2 ЕСТЬ origin; ночь-3 хэндофф врал про rsync).
   - Миграции накатились авто (planner-migrate, api depends_on success): `d4e5f6a7b8c9→e5f6a7b8c9d0(fork0)→f1a2b3c4d5e6(impact)→a7b8c9d0e1f2(habits/metrics)`. DB rev = `a7b8c9d0e1f2`.
   - Проверено: `/api/health` 200, `/api/habits` 401 (роут жив), app 200, bundle прод=локал `index-DA-4UJD0.js`.
3. **T1 «Задачи» добита до V3-мокапа** (`4dc313d`, bundle `index-CP5a8L3E.js`) — поэтапная код-фаза по страницам:
   - **G1 heat-датапикер (T1·B):** бэк `services.tasks.day_density` (open-задачи/день → g/y/r; red = ≥5 ИЛИ прошлый день с открытым high; ZERO-AFK чистый расчёт) + роут `GET /api/tasks/density?from&to` (объявлен ПЕРЕД `/{task_id}` — иначе 422). Фронт: `api.getDensity`, DateJumpSheet грузит heat месяца → классы `heat-g/y/r`, кольцо «сегодня» (inset ring под заливкой). 7 тестов (5 сервис + 2 API).
   - **G2 all-day «+N ещё» (A1):** 2 чипа + разворот скрытых, `.cal-chip-more` (пунктир).
   - **G4 состояния T1 под мокап:** error (SVG-треугольник + подзаголовок + «Повторить»), list-empty (SVG-smiley + «День свободен» + «Добавить задачу» → фокус quick-add). `.state-stub`. Убран неиспользуемый импорт Empty.
   - Гейт: 160 pytest · 35 vitest · tsc 0 · build.
   - **Визуал-самопроверка:** `frontend/preview-t1-mock.html` (мок-fetch density) + chrome-devtools скрины — heat-цвета совпали с мокап-палитрой (rgba green/yellow/red), +N-чип, оба состояния. Задеплоено, проверено: density 401 (роут жив), bundle прод=локал.

## Решения
- **Порядок код-фазы:** поэтапно по страницам V3, владелец гейтит. Начали с T1.
- **T1 цвет метки этапа:** оставлен статус-цвет (C2, уже собран, владелец видел) — НЕ переключал на C1-нейтральный, хотя реестр писал C1. Конфликт мокап-vs-реестр, выбран реализованный.
- **Свайпы в T1 tasks-view НЕ делал:** TaskItem умеет, но мокап T1·D их не требует (вне контракта Форка A).

## Грабли/инсайты
- Density-роут `/api/tasks/density` ОБЯЗАН быть объявлен до `/api/tasks/{task_id}` в том же APIRouter (FastAPI матчит по порядку; иначе "density" летит в int-параметр → 422).
- Превью Sheet-датапикера растягивается на всю ширину вьюпорта (portal-оверлей) → огромные круги-дни в preview; на реале 390px = норм ~50px. Не регрессия, артефакт превью.
- Локальный бэк-venv = `planner-v2/.venv` (НЕ общий `~/ИИ-агенты/venv` — там нет pytest).

## Открытые нити (продолжить отсюда)
- **⚠️ Telegram визуал-гейт владельца** по деплою ночь-3 И по T1 (датапикер-heat, +N, состояния) — открыть Mini App живьём.
- **Следующая страница = T2 Календарь** (бэк Форк B готов: range + `/api/milestones`; фронт React не собран — Неделя/Месяц/Лента). Разведка T2 (мокап vs код → дельта) — заходить как с T1.
- Дальше по очереди: T4 Цели (бэк Форк0) → T3 Гант (нужны drag-эндпоинты) → INBOX (suggested_stage_id + эвристика) → DETAIL-фронт (блоки Этап/Вклад в самой детали) → Tracking деталь-экраны.

## ── HANDOFF 2026-06-12 → завтра ──
Снимок прода = `4dc313d` (ветка planner-v2-phase0, origin sync). Деплой ночь-3 + T1 ЖИВЫ на проде, миграции `a7b8c9d0e1f2`. Гейт зелёный (160 pytest · 35 vitest · tsc0 · build). **Старт завтра: T2 Календарь** — разведка дельты мокап↔код (2 Explore-агента параллельно, как делали для T1), потом TDD-сборка → визуал preview → деплой. Правила: мокап=контракт, визуал перед «готово», ZERO-AFK (бэк не зовёт LLM), русский. Деплой = git push + ssh pull + compose build (НЕ rsync).

## Инструменты/системы
planner-v2 (FastAPI+PG+React/Vite), prod Hetzner `root@188.245.42.4:/root/planner-v2-src`, GitHub origin, chrome-devtools (визуал-гейт), мокапы `база-проекта-v3/`.
