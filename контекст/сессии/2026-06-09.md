# Сессия 2026-06-09 — inline-создание в ячейке + правки + delete

## Цель
Возврат к разработке planner-v2 (ветка planner-v2-phase0). Старт — жалоба «дизайн ≠ мокап», итог — собрана и отполирована фича inline-создания задач на таймлайне + починен delete.

## Сделано (всё задеплоено на prod, bundle сверялся каждый раз)

### 1. Фикс all-day чипов Today по канону (commit fc2f768)
Чип был `<button>` без сброса нативных стилей → текст падал в системный iOS-синий, фон серый, торчала рамка. Канон P01: фон=тинт проекта (.13), текст=цвет проекта, кант слева=приоритет, лейбл «весь день». theme.css `.cal-chip` сброс appearance + `.cal-allday-label`/`.cal-allday-chips`; Today inline-тинт/цвет/кант. Визуал-проверен статичным preview на реальном CSS.

### 2. ВИЗУАЛ-ФИДЕЛИТИ ГЕЙТ заведён (процесс)
Корень дрейфа «мокап≠деплой»: гейт деплоя был bundle-hash+health, без сравнения с мокапом. `база-проекта/` = КОНТРАКТ сборки. Зафиксировано: `planner-v2/.claude/TEAM.md` (Фаза 3 → ВИЗУАЛ-ФИДЕЛИТИ ГЕЙТ) + память [[feedback-mockup-fidelity-gate]].

### 3. Inline-создание задачи в ячейке таймлайна (фича, Фаза 1→2→3)
Полный цикл по анти-проёб киту:
- **Фаза 1 разведка:** brainstorm → deep-спека `docs/superpowers/specs/2026-06-09-inline-cell-create-deep-spec.md` (FSM, edge E1–E12, reuse-карта) + мокапы `design/inline-cell/flow-mockup.html` + `annotated-spec.html` + `overlap-options.html`. Развилки владельца: жест=тап+протяжка(Apple); E12=авто-коммит; наложения=колонки(Apple).
- **Адверсариальный обзор** (codex CLI нет → 2 Claude-агента: дизайнер+инженер): 4 крит-дыры (жест/клавиатура/API/SAVING-гонка) закрыты в спеке §11. Готовность 4→9/10.
- **План TDD** `docs/superpowers/plans/2026-06-09-inline-cell-create.md` (8 задач).
- **Сборка субагентами** (subagent-driven, спека+качество-ревью): vitest заведён (его не было), `lib/timelineLayout.ts` (чистые функции + 13 юнит-тестов: pointerToMinutes/normalizeRange/layoutColumns), DayTimeline (колонки+жест+draft), Today (createTask+scroll-lock), App (убран bottom-sheet). E9+M3 (таб-смена коммит / таймаут 5с) дозакрыты.

### 4. Итеративные UX-правки по живым жалобам владельца (Telegram)
- «весь экран колдоёбит при тапе» → **3 корня:** жест создания перехватывал скролл (→ long-press-гейт: драг=скролл, тап=1ч); scroll-lock body:fixed рвал экран (убран — черновик в потоке); scrollIntoView на каждый ререндер (убран). (commit 894a040)
- «Готово+клавиатура, лезет навбар» → при черновике прячем `.tabbar`+`.fab` через `body.inline-draft`. (82d1f36)
- «хочу сначала двигать потом растягивать» → long-press двигает 1ч-блок (выбор времени), длину тянешь за края черновика (onDraftResize). (c46cd83)
- «черточки скрыть, активны при зажатии» → грипы скрыты, видны только у активного (long-press) блока; ресайз-полосы не рендерятся у неактивных (нет dead-зон скролла). (e95e46e)
- «long-press открывает задачу на отпускании» → suppressClickRef гасит synthetic click. (ce51f4a)

### 5. Кнопка удаления задачи — починена (commits 1e2885d фронт, 24db723 бэк)
**2 корня:** (а) `window.confirm` в Telegram WebView не работает → заменён на нативный `tg.showConfirm` (TaskDetail confirmDelete + фолбэк); (б) **в бэкенде НЕ БЫЛО роута DELETE /api/tasks/{id}** — реализован `svc.delete_task` (сабтаски каскадом, теги явно, checkitems/reminders ORM-каскад) + роут 204 + 2 теста. **107 pytest зелёных.** Проверено: DELETE без auth → 401 (роут есть). Владелец подтвердил: работает.

## Файлы (ключевое)
`frontend/src/lib/timelineLayout.ts(+test)` · `components/DayTimeline.tsx` · `screens/Today.tsx` · `App.tsx` · `theme.css` · `components/TaskDetail.tsx` · `src/planner/services/tasks.py` · `src/planner/api/routes/tasks.py` · `tests/test_api_tasks.py` · `vitest.config.ts`.

## Деплой-факты (подтверждены этой сессией)
prod = `/root/planner-v2-src`, ветка planner-v2-phase0. Деплой = push origin → ssh root@188.245.42.4 → `cd /root/planner-v2-src && git pull --ff-only && cd planner-v2 && docker compose -f docker-compose.prod.yml up -d --build planner-api`. Проверка = live bundle-хэш == локальный build + `/api/health` 200. Фронт собирается В образ. Последний bundle: `index-Dp-MhHu9.js` (после delete-фикса фронт не менялся; backend пересобран 24db723).

## ── HANDOFF ──
- **Фича inline-создание + колонки + delete — ЖИВЫ на проде, владелец подтвердил.** Жесты: тап=1ч, long-press=двигать/активировать+ресайз краёв, драг сетки=скролл, тап блока=деталь.
- **Мокапы база-проекта синхронизируются** под новый функционал (фоновый агент: P01-segodnya + F11-daytimeline → inline-create/колонки/грипы-на-зажатие). Проверить результат агента.
- **Telegram WebView граба:** `window.confirm`/`alert` не работают → всегда `tg.showConfirm`/`showAlert`. Записать в learnings.
- **Дальше (открыто):** Δ1 из дифа «таб Списки → Обзор» (финальный мокап перевёл, код на Списках) — крупное; либо новая волна (Цели/Трекинг); либо довести остальные экраны до мокап-канона через визуал-фиделити гейт.
- **Не путать:** prod=`/root/planner-v2-src`; деплой=git pull+compose build.
