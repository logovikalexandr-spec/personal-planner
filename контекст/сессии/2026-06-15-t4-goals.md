# Сессия 2026-06-15 — Блок B: T4 «Цели» добит до мокапа

> Worktree `planner-wt-t4`, ветка `feat/t4-goals` (от `d934e7b`, WIP-предок `35a10f1`).
> Параллельно идут T2 (`feat/t2-calendar`) и T3 (`feat/t3-gantt`) — НЕ трогать.

## ── HANDOFF → следующая сессия ──

### СДЕЛАНО (коммит `1d1dcac`, всё зелёное, НЕ задеплоено)
Заглушка `Goals.tsx` → полноценный экран Цели по контракту `база-проекта-v3/pages/T4-celi.html`.

5 файлов (theme.css аддитивно, остальное — мои):
- **`src/theme.css`** — секция `t4-*` (`.t4 .sub/.seclbl`, `t4-pulse*`, `t4-card`+`::before` через `--c`, `t4-ring`, `t4-stages i/.on`, `t4-next`, `t4-ai.fast/.risk`, `t4-ai-ic`, `t4-newbtn`). Всё на токенах DESIGN (`--bg/--surface/--text-muted/--accent/--accent-soft/--danger/--success/--ring-track/--border`). `project.color` → inline `--c`. Дописал в КОНЕЦ файла (чужие классы целы).
- **`src/screens/Goals.tsx`** — фикс верности: `late`-этап теперь = активный (`activeIdx` = `current||late`) → `этап N`, след.веха-блокер, подсветка полоски. empty/error переведены на общий `.state-stub/.state-ico/.state-title/.state-sub` + `.lnk` (reuse, меньше CSS).
- **`src/preview-t4-mock.tsx`** + **`preview-t4-mock.html`** — stateful fetch-мок, сид = мокап (3 проекта · 2 в графике · 1 отстаёт). Параметры: happy / `?state=empty` / `?state=error`.
- **`tests-e2e/T4-celi.spec.ts`** — 12 тестов ×2 браузера (A1-A8 + состояния + токен-смоук + визуал-baseline).

### ГЕЙТ (зелёный, проверено)
`lint:tokens PASS · 48 vitest · tsc 0 · 24 e2e функц. · coverage 8/8 ecode, 0 дыр`.
Визуал-сверка preview↔мокап пройдена (chrome-devtools, 3 состояния — happy/empty/error совпали).

### ⚠️ ГОЧИ (важно для следующей сессии)
1. **Worktree-путаница:** задача говорила «папка planner-wt-t2», но T4 = **planner-wt-t4** (`feat/t4-goals`). t2 = чужой блок Календарь. Работать в t4, иначе клоббер.
2. **node_modules в worktree нет** — свежий worktree. Перед гейтом: `cd planner-v2/frontend && npm ci`.
3. **Гейт-баг с сервером:** дефолтный `npm run e2e` бьёт `localhost:5173`, где висит dev-сервер **main-репо** (`personal-planner/...`, НЕ worktree, pid живёт) → SPA-фолбэк, ВСЕ тесты падают ложно. Гонять против своего сервера:
   `npx vite --port 5191 --strictPort &` → `PLAYWRIGHT_BASE_URL=http://localhost:5191/app/ npx playwright test T4-celi`
   Свой сервер гасить после; чужой 5173 не трогать.

### ЧТО ОСТАЛОСЬ (выбор владельца)
**A. Отдать в прод (через координатор-чат, прод один — serial deploy):**
   1. Утвердить baseline визуально (агент эталон НЕ генерит):
      `npx playwright test T4-celi --update-snapshots --project=chromium`
   2. Сигнал координатору → merge `feat/t4-goals` + deploy (по очереди с T2/T3, не параллельно).

**B. Строить дальше в T4 (этот worktree):**
   - **T4d детальный экран** — тап карточки → этапы→задачи→разбор ИИ. Мокап `база-проекта-v3/pages/T4d-celi-detail.html` готов.
   - **Бэк-долг для T4d** (сейчас НЕ построен): недельная дельта %шанс (нет истории замеров) + структурный разбор AI-заметок (`ai_notes` плоский список → нужна структура B1-B5). T4-СПИСОК этого не требовал, T4d-ДЕТАЛИ требуют.
   - Доточить незакрытые флоу из `T4-flows.html` / `T4-состояния.html` если есть.

### НЕ ДЕЛАТЬ
- НЕ деплоить без сигнала (прод один).
- НЕ генерить baseline-эталон (правило: только человек, анти snapshot-fatigue).
- НЕ править `memory/project-state.md` из этого worktree (общий файл, параллельные блоки тоже пишут → конфликт; координатор сводит при мерже).
- Рецепт «добить экран» — `контекст/конвейер-плейбук.md`.
