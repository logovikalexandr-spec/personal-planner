# Сессия 2026-06-14 (вторая) — конвейер верности построен + T1/нав добиты + ДЕПЛОЙ

> Реализационная сессия после дизайна конвейера (см. 2026-06-14.md). Построили инфру против дрейфа «мокап≠деплой», добили страницу Задачи + нав-бар под мокап, задеплоили на прод.

## Что сделали (коммиты на ветке planner-v2-phase0)

1. **Слой предотвращения** (`5ca1571`): `tools/check-tokens.sh` (гард сырого hex + битых var, zero-dep, `npm run lint:tokens`). +3 токена (`--success/--project-tag/--ring-track`). **Гард сразу нашёл 5 тихих багов цвета** (var(--onyx/red/steel/hairline)+дубли) — починены.
2. **Playwright-скелет** (`61acfd5`): config 390×844 chromium+webkit, `_telegram.ts` стаб, smoke. `updateSnapshots:"none"` (агент не трогает baseline).
3. **Гейт на T1 timeline** (`6781013`): `T1-today.spec` 14 тестов (структура+токен-смоук+визуал-baseline) + `coverage.sh` + data-testid в DayTimeline.
4. **Доки конвейера** (`53e0602`): спека v2 + план gate1 v2; v1 заархивирован ⛔.
5. **Полное покрытие T1** (`951d925`): `preview-t1-full-mock` (весь Today + stateful fetch-мок) + `T1-full.spec` (A1-A5 хром + draft + persist + drag snap-15) + `page.clock` для детерминизма baseline.
6. **Аудит дрейфа** (`a2115bd`): `preview-app-mock` (весь App с мок-сетью) + [инвентарь](../аудит-дрейфа-2026-06-14.md). Вывод: **структурный бэклог**, не косметика.
7. **Дрейф-фикс FAB+подсказка** (`1f7dd20`): пилюля «Задачи»→«Список»+иконка+позиция впритык к [+]; подсказка «‹›свайп — другой день». Залочено.
8. **Нав-бар IA** (`bdeac65`): «Сегодня»→«Задачи», «Списки»-таб→«Гант» (+IcoGantt, стаб-экран; Списки в бургере). `app-nav.spec` на РЕАЛЬНОМ App.
9. **Датапикер T1·B** (`4fc72d5`): бот-шит→поповер под чипом, круги→квадраты-heat, 2 кнопки→1 (тап применяет). Залочено.
10. **Деплой на прод** (`06d2060` доки): rsync+build+up. **Live на `planner.188.245.42.4.nip.io/app/`** — нав+FAB+пикер видны. ledger не тронут, фронт-only.

**Гейты на момент конца:** lint:tokens · 35 vitest · 52 e2e (×2 браузера) · tsc · coverage T1 7/7. Build чистый (`index-Cch6KAhO.js` = на проде).

## Ключевые выводы (зачем это всё / границы)
- **Инфра работает** — проверена изломом: впихнул баг (hex/битый var/цвет/ярлык/непокрытый ecode) → каждый гейт покраснел (4/4).
- **Почему ошибки всё равно были:** (1) инфра новая, покрывала только T1; (2) гейт не запускается сам (нет CI/hook); (3) **baseline сверяет код с собой (регрессия), НЕ с мокапом** — «совпадает с мокапом» пока ловят глаза человека (FAB/пикер нашёл владелец, не гейт); (4) часть «ошибок» = **непостроенные экраны** (Цели/Календарь-виды/Гант) — гейт не защищает то, чего нет.
- **«Переписать всё по мокапу» = медленнее**, чем чинить: бэкенд + ~60% фронта (T1, DayTimeline-драг, composer, тема) корректны и дороги к повтору; непостроенное строится одинаково в обоих сценариях. Решение: конвейер по-экранно (keep/fix/build), не teardown.
- **Прод URL = nip.io**, не sslip.io (CLAUDE.md был неправ → ложная «прод упал»; на деле TLS sslip без серта).

## ── HANDOFF ── (следующая сессия)

**Старт:** читай [конвейер-плейбук.md](../конвейер-плейбук.md) (turnkey-рецепт) + [аудит-дрейфа](../аудит-дрейфа-2026-06-14.md) (что осталось).

**Делать (по приоритету, владелец выбрал «строить непостроенное по мокапу»):**
1. **T4 Цели** (быстрый win, сейчас заглушка «следующей фазе») — мокап `T4-celi.html`/`T4d-celi-detail.html`/`T4-flows`/`T4-состояния`. Бэк Форк0 готов (success%/stages/ai_notes). По плейбуку: preview+spec+coverage+эталон → деплой.
2. **T2 Календарь** Неделя/Месяц/Лента (сейчас вид одного дня; экрана недели НЕТ — `weekLayout.ts` хелперы есть). Мокапы `T2a/b/c`.
3. **T3 Гант** (стаб-таб стоит) — мокап `T3a/b`.
4. **Error boundary** на экраны (дешёвая страховка: кривое поле → белый экран, как Привычки в app-mock-моке).
5. Доаудит шторки(F17)/DETAIL/INBOX (второй проход).
6. **TODO инфры:** вшить авто-запуск гейта (pre-commit hook + CI) — сейчас ручной.

**Эталон под защитой:** T1 «Задачи» (timeline+хром+пикер, 7/7 ecode) + нав-бар — НЕ переписывать, копировать как образец.

**Деплой:** процедура в плейбуке (rsync→build→up, URL nip.io, проверка снаружи).
