# Сессия 2026-06-15 — координатор: T1-доводка + datetime + параллель T2/T3/T4

> Главный чат = **координатор** (ветка `planner-v2-phase0`, worktree `personal-planner`).
> Параллельно 3 воркер-чата в worktree (см. ниже). Всё ниже — ЗАДЕПЛОЕНО на прод.

## Прод сейчас
- URL `https://planner.188.245.42.4.nip.io/app/`, health 200, bundle **`index-DzivQVfq.js`**.
- Содержит: **T1-правки (мои) + T2 Календарь** (после merge). Деплой = rsync `frontend` из координатор-worktree → build → up.
- ⚠️ Telegram кэширует Mini App → после деплоя «Закрыть» приложение/очистить кэш Telegram, иначе видно старое.

## Сделано координатором (ветка phase0, коммиты по порядку)
1. `d934e7b` **Пикер «Дата/время»** (TickTick старт→дедлайн): `Task.end_date` (миграция `b9c0d1e2f3a4`), рерайт DateSheet (heat-спан/весь-день/метка длительности), 5 консумеров, многодневный=чип all-day, `lib/datetime`. + **drag/click фиксы «Тоо»** (см. урок ниже) + создание только по long-press + убран свайп-день+подсказка.
2. `0bf4c7f` короткий блок (≤30мин) — заголовок по центру, время скрыто.
3. `1b47965` **деталь: действия в ⋯ меню** (нижний бар убран) + «Won't Do»→«Не буду делать» + preview-detail-mock + DETAIL-menu.spec.
4. `9cb29aa` деталь: «Сабтаски»→«Подзадачи».
5. `6d244a7`→`b964650`→`ecb494e` **ручки ресайза = кружки** (9px, accent, по диагонали верх-право/низ-лево).
6. `6ec13d8` **Список (Today tasks-view): «Просрочено» под шторку** (свёрнуто) + **группировка по проекту** (`lib/groupByProject`+vitest) + «Без времени·N».
7. `3b7040e` **merge `feat/t2-calendar` → phase0** (конфликтов нет, theme.css аддитивно). Гейт: tsc·lint·vitest 53.

## Корневой баг «Тоо» (урок, в памяти `planner-now-line-pointer-bug`)
Блок не кликался/не двигался телом, resize краёв работал. ДВА дефекта: (1) `.cal-now` (now-линия) без `pointer-events:none` перехватывала указатель в центре блока; (2) `.cal-block-main` (ручка) не на всю высоту (`align-self:stretch`) + нет `touch-action:none`. Детектор: `elementFromPoint(центр)`. CDP-синтетика НЕ репродит iOS-скролл/позицию now-линии → верифай через `page.clock` + изолированный порт.

## Параллельная сборка (worktree-изоляция)
| Блок | Worktree | Ветка | Статус |
|---|---|---|---|
| T1 (координатор) | `personal-planner` | `planner-v2-phase0` | ✅ задеплоено |
| T2 Календарь | `planner-wt-t2` | `feat/t2-calendar` | ✅ слито+задеплоено (Неделя/Месяц/Лента, 4 коммита) |
| T3 Гант | `planner-wt-t3` | `feat/t3-gantt` | ⏳ в работе (бэк готов: `PUT /api/stages/{id}`+`PATCH /tasks`) |
| T4 Цели | `planner-wt-t4` | `feat/t4-goals` | ⏳ в работе (Goals на `.state-stub`, preview-t4) |

**ПРАВИЛО ДЕПЛОЯ (выучено болью):** прод один. Воркеры **НЕ деплоят** — только коммит в свою ветку → координатор `merge feat/* → phase0` → деплой. Иначе деплой воркера затирает чужое (T2 затёр T1, потом наоборот; починено merge). Брифы воркеров: `контекст/параллель/брифинг.md` в каждом worktree.

**ГОЧА портов:** все worktree гоняют vite на 5173; playwright `reuseExistingServer` цепляется к чужому серверу → локальные e2e/визуал тестируют ЧУЖОЕ дерево. Координатор верифает на изолированном порту (vite `--port 5266` + standalone playwright). TODO: дать каждому worktree свой порт.

## ── HANDOFF → новая сессия ──
- Прочитать: этот файл + `memory/project-state.md` (верхний блок) + брифы воркеров.
- Прод жив (DzivQVfq), T1+T2 в нём.
- Открыто: домержить T3/T4 когда сдадут (по правилу деплоя). Прописать per-worktree порты.
- Бэклог: T1-today/full 3 визуал-baseline стейл (владелец `--update-snapshots` после Telegram-ОК); error-boundary; доаудит INBOX.
