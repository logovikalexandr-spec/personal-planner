# Сессия 2026-06-20 — ФИНАЛ planner: T3+T4 на проде

## Цель
«Доводим planner до конца, до рабочего финала.» На входе: T1+T2 на проде, T3 Гант + T4 Цели собраны в worktree-ветках, НЕ слиты/НЕ задеплоены.

## Сделано
1. **Мерж T3 → phase0** (`17ce259`). Конфликт только `theme.css` (T2-секция HEAD vs T3-секция). Резолв = оставить обе.
2. **Мерж T4 → phase0** (`1a40dad`). Тот же `theme.css` (T2+T3 vs T4). Резолв = три секции подряд T2→T3→T4. Gantt.tsx/Goals.tsx — per-screen, без конфликтов.
3. **Гейт** (всё локально, зелёное): `lint:tokens` PASS · `tsc --noEmit` 0 · `vitest` 66 passed (вкл. ganttLayout 13) · `vite build` чист (bundle index-Cq_chkmP.js) · `e2e` **104 passed / 12 skipped / exit 0** (T3+T4 функц.+токен-смоук на chromium+webkit; 12 skip = визуал-baseline T3/T4 без эталона — по плейбуку агент эталон не трогает).
4. **Деплой** frontend-only rsync → `docker compose -f docker-compose.prod.yml build && up`. Бэк не трогал.

## Верификация прода (снаружи)
- `curl /api/health` → **200**
- прод-бандл `index-Cq_chkmP.js` = локальный build-хэш (совпал)
- маркеры в живом бандле: `g-seg` (T3), `t4-card`/`t4-pulse` (T4)
- прод alembic head `b9c0d1e2f3a4` — downstream от всех миграций (Форк0 AI-fields/impact/habits/stages) → бэк полный, эндпоинты T3/T4 живые

## Итог
5 табов на проде одним build без клоббера: **Задачи · Календарь · Гант · Цели · Привычки.**

## ── HANDOFF ──
Код-финал достигнут. Осталось ТОЛЬКО человеку:
1. Визуал-гейт в Telegram (Гант+Цели глазами под мокап T3a/b + T4-celi).
2. После ОК — владелец сам обновляет baseline: `npx playwright test T3-gantt T4-celi --update-snapshots --project=chromium` + коммит эталонов (12 skip → зелёные).
3. Опц. `git worktree remove planner-wt-t3 planner-wt-t4`.
Бэклог (не блок): per-worktree vite-порты, error-boundary, Волна-2 T3 драг/pinch (COVERAGE-DEFER), стейл T1 baseline.
