# Сессия 2026-06-20 (вечер-3) — Календарь: вид «Дни» + полировка всех видов

## Что сделано (всё ЗАДЕПЛОЕНО, мокап-first соблюдён)

**Финальный прод-бандл:** `index-DV6OceBY.js`, health 200. Ветка `planner-v2-phase0`. Серия коммитов от `7d0f92a`.

### 1. Новый вид «Дни» (2/3/4/7) вместо таба «Неделя»
- Сегмент: **Дни · Месяц · Лента** (таб «Неделя» удалён).
- Степпер **2/3/4/7**, дефолт 2, localStorage `cal.dayCount`. «7» = неделя Пн–Вс.
- `‹ ›`: 2/3/4 сдвиг на N дней, 7 — на неделю.
- Колонка 2/3/4 = переиспользованный **DayTimeline (compact)** → drag внутри дня бесплатно (боевой жест Today). Реюз-инсайт: drag = iOS-рискованный кусок → реюз проверенного DayTimeline, обвязку (all-day/вехи) портировал.
- **«7» = оригинальный `CalendarWeek`** (НЕ compact×7 — он нечитаем; владелец забраковал). Условный рендер `dayCount===7`.
- Тап дня (число/колонка) → таб «Задачи» с датой (App.gotoDay→Today.gotoISO).
- Контракт: `база-проекта-v3/pages/T2d-calendar-days.html`. План: `plans/2026-06-20-calendar-days-wave1.md`.

### 2. Полировка (по живым скринам владельца на iPhone)
- Тап задачи в Календаре → **полноэкранная деталь** (`.detail-overlay`), не в потоке снизу.
- Compact-блок **непрозрачен** (`::before` подложка `var(--surface-2)`) — лейбл часа не просвечивает сквозь тинт.
- **Ряд навигации переделан** (мокап `T2d-navrow.html`): «Сегодня» вынесена в шапку (справа от заголовка); степпер крупный, во всю ширину ряда равными кнопками, активный = ember-заливка; дата сжата (min под длинный месяц).
- **Лента:** «Просрочено» сворачивается (шеврон) + **каждый день** сворачивается (шеврон, пустые без шеврона); задачи = общий **TaskItem**; свайп-действия Готово/Изменить/Удалить.
- **Месяц:** убрана цвет-легенда; панель дня = общий TaskItem.
- Карточки в Месяце/Ленте — отступ `var(--s2)` между ними (как в списках, не слипаются).

## Гейты (каждый деплой)
tsc 0 · lint:tokens PASS · vitest 68 · e2e функц. ~85 passed (T1-T4+nav+drag, регрессий нет). Самопроверка chrome-devtools перед каждым деплоем.

## Файлы
`CalendarDays.tsx`(new) · `CalendarWeek.tsx`(легенда убрана, оживлён для 7) · `CalendarMonth.tsx`(TaskItem+legend) · `CalendarAgenda.tsx`(TaskItem+collapse+swipe) · `DayTimeline.tsx`(+compact/startHourOverride, Today не затронут) · `Calendar.tsx` · `App.tsx`(gotoDay) · `Today.tsx`(gotoISO) · `calDates.ts`(+daysList+test) · `types.ts` · `theme.css` · e2e T2.

## DEPLOY-ГОЧА (подтверждена снова)
build-секция у **`planner-migrate`** (api/bot БЕЗ неё, образ `planner-app:latest`). Деплой = rsync frontend → `docker compose build planner-migrate` → `up -d --force-recreate planner-api planner-bot`. Билдить planner-api = no-op!

## ── HANDOFF ──
Всё по Календарю задеплоено и проверено владельцем по ходу. iOS-жесты (drag блока, свайп задач) — проверяются на устройстве (desktop preview не воспроизводит).
**Открыто/бэклог:** cross-day drag, драг из/в лоток (снять/поставить дату), свайп-шторки (edge-swipe УЖЕ в коде App.tsx — разобрать на устройстве, не баг). Визуал-baseline T2 (`T2-days2/days7-happy.png` и др.) владелец переснимет: `npx playwright test T2-calendar --update-snapshots --project=chromium`. Параллельная сессия владельца тоже трогала planner (DayTimeline/Today/смарт-списки) — рабочее дерево с чужими незакоммиченными правками, rsync шлёт как есть.
