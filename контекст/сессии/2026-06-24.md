# Сессия 2026-06-23/24 — workout-лог доводка + сага «футер всплывает»

## Сделано (ЗАДЕПЛОЕНО на прод)
1. **«0 мин в зале» убрано** — из итогов недели и истории сессий workout-лога (длительность нигде не засекалась → всегда 0). `WorkoutLog.tsx`, `workout-log` бэк-поле `duration_minutes` оставлено в БД, в UI не показывается.
2. **Тренд-стрелка ▲/▼ у «65% ШАНС»** на карточке Целей (стиль метрик: ▲ зел / ▼ красн + дельта). Решение владельца: **ручной пересчёт** (Claude пишет в БД). Реализация:
   - Миграция `d2e3f4a5b6c7` — колонка `project.success_probability_prev` (nullable).
   - `update_project_ai` авто-сдвигает prev=старое при смене success_probability (тренд считается из разницы). +тест `test_update_project_ai_shifts_prev_on_change` (pytest зелёный).
   - `ProjectOut`+`_to_out`+frontend `Project` тип+`Goals.tsx` рендер стрелки.
   - Стрелки пока НЕТ (prev пустой — шанс ещё не пересчитывали). **Пересчёт шанса — к концу недели** (договорённость).
3. **Поправлен protух `инструкции.md`:** prod-домен `sslip.io` → `nip.io`.

## Сага «футер «Завершить» всплывает при правке числа» (~16 деплоев, ЧАСТИЧНО)
Полный разбор + корни + что не сработало → harness-память `[[planner-workout-footer-keyboard-bug]]`.
Кратко: iOS PWA `fixed inset:0`=812 vs `100dvh`=874; vv качается 812↔874; letterbox красит canvas-фон (CSS не достаёт); при фокусе iOS скроллит fixed-оверлей → футер уезжает в середину.
**Текущее (бандл `index-CpoiW-dl.js`):** футер=flex-элемент в оверлее `height:100dvh`; таб-бар+футер фон `--bg` (полоса под баром ушла ✅); условный сайзинг оверлея под vv ТОЛЬКО при клаве → футер над клавой. **«Уже лучше, но не до конца»** — остаточный сдвиг при правке. Запаркован.

## В коде осталась диагностика (снять при доводке бага)
- `WlDebugOverlay` (флаг `wl-debug` / `?wldebug` / 3 тапа по шапке трени) — числа вьюпорта.
- `BUILD_TAG` в шапке трени (хэш загруженного бандла) — проверка свежести.

## ⚠️ Весь воз UNCOMMITTED → закоммитить
~26 файлов изменено на ветке `planner-v2-phase0` (деплой=rsync, не git). Коммит в конце сессии.

## ── HANDOFF ──
**Открыто:**
1. **Футер трени — добить** (приоритет): остаточное всплывание при правке. Нужен device-замер vv/footer В МОМЕНТ открытой клавы (через `?wldebug`), допозиционировать; потом снять диагностику (WlDebugOverlay+BUILD_TAG).
2. **Пересчёт «шанса»** к концу недели → появится стрелка ▲/▼.
3. **Мелочь workout** (из `[[planner-sport-trener]]`): кнопка «новое упражнение»→«старт»; seed rep-ranges синк 8–12; недельный разбор тренера.
