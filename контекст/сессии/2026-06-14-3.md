# Сессия 2026-06-14-3 — creation-flow добит + дизайн «Дата/время»

## Что сделано (всё на проде, серия фиксов T1 «создание задачи»)

Стартовали с «доводим T1» (T4 Цели начат → сохранён WIP `35a10f1`, отложен). Дальше — поток правок по живым скринам из Telegram:

- **Хинт пустого дня убран** (`ed8a073`) — пустой день = чистая сетка.
- **05:00 не режется** липкой шапкой (`44a3a7d`) — `.cal-grid margin-top` (margin двигает rect.top, жест не ломается).
- **Тап ячейки часа → блок на НАЧАЛО часа** (`d77857a`) — был баг «середина ячейки → :30»; floor к часу для тапа, snap15 для драга.
- **iOS fixed-баг шитов/баров** → общий хук `lib/viewportAnchor.useBottomAnchor` (top-якорь по visualViewport): бар «Готово» (`5ba2d93`) + quick-overlay (`8363c72`). Чёрный «+» FAB → белый (`8363c72`).
- **Черновик не подъезжает под бар «Готово»** (`addc783`) — доскролл + scroll-margin.
- **Sheet позиционируется из visualViewport** до первой отрисовки (`88961b1`, `feaac00`) — шиты не «прыгают из центра».
- **КЛЮЧЕВОЕ — два поля создания → одно:** владелец психанул («крутятся два поля»). Откат FAB→TaskComposer (`626dbc0`), затем **TaskComposer-из-FAB удалён полностью** (`7417ae9`). «+» = ЕДИНСТВЕННОЕ быстрое поле.
- **Чипы быстрого поля = рабочие пикеры ИНЛАЙН** (`d83f0a1`): дата/приоритет/тег → DateSheet/PriorityPicker/TagPickerSheet поверх оверлея (z-index шитов 70). Проверено e2e ПЕРЕД деплоем.
- **Чип «проект»** (`85b3ffc`) → ProjectPickerSheet. **«+» без клавиатуры** (убран autoFocus, фокус на FAB). Проверено e2e.
- **Быстрые-чипы даты** (Сегодня/Завтра/След.понедельник) убраны из DateSheet (`04cc14a`).
- **DateSheet-календарь** красит дни по нагрузке (heat g/y/r) через getDensity — как мини-календарь.

## Дизайн «Дата/время» (мокап-first, УТВЕРЖДЁН владельцем «мне нравится»)
Вернулись к процессу (хватит вслепую). Brainstorm → мокап → утверждение.
- Мокап `база-проекта-v3/pages/T1-datetime.html` (A одиночный день, B многодневный) — переделка «Длительность» → единый пикер старт(дата+время)→дедлайн(дата+время), heat-календарь, весь-день, длительность-метка, напоминание/повтор. БЕЗ вкладок.
- Мокап `база-проекта-v3/pages/T1-quickadd.html` — синк реальности быстрого поля.
- Спека `planner-v2/docs/superpowers/specs/2026-06-14-datetime-picker-rework-design.md`.
- Коммит дизайна `3aaf143`. State-коммит `961843c`.

## Уроки сессии
- **Хватит угадывать по скринам.** Половина боли = слепые фиксы. Со второй половины: e2e-репро/проверка ПЕРЕД деплоем (verify-спеки), мокап-first для фич. Владелец явно потребовал.
- iOS Telegram WebView: `position:fixed + bottom + kb-inset` ненадёжен с клавиатурой/скроллом → позиционировать из `visualViewport` (top-якорь / inline padding до первой отрисовки).

## ── HANDOFF → НОВАЯ СЕССИЯ (2 задачи, обе КОД) ──
Полный контекст: `memory/project-state.md` (верхний блок). Прод жив, creation-flow готов.

**Задача 1 — Переделать «Длительность» → пикер «Дата/время».** Дизайн утверждён.
- Читать: спеку `docs/superpowers/specs/2026-06-14-datetime-picker-rework-design.md` + мокап `T1-datetime.html`.
- Суть: переписать `DateSheet.tsx` (убрать вкладки Дата/Длительность, DURATIONS, гейт) → старт+дедлайн+heat-календарь+весь-день+длительность-метка. **Бэк: +`Task.end_date` (миграция add-column)** + TaskOut/In/Patch + DateValue. Таймлайн: один день = блок (как есть); многодневный = чип в all-day (полные спан-бары = follow-up).
- Гейт: vitest (длительность-метка, выбор начало/дедлайн) + pytest (round-trip end_date) + e2e + lint:tokens + tsc. Деплой: rsync frontend → build → up (плейбук). НЕ вслепую — verify перед деплоем.

**Задача 2 — БАГ drag наложенных блоков.** Блок в колонке 0 двигается long-press'ом, блок в колонке 1 (наложение по времени) НЕ двигается (resize краёв при этом работает). Move-хендлеры (`onBodyDown`/`onDragMove` в `DayTimeline.tsx`) идентичны для обеих колонок → корень не виден чтением (подозрение: гонка pointer-capture/ре-рендер). **Построить репро-харнес** (preview с 2 наложенными блоками) → воспроизвести → root-cause → фикс. НЕ блайнд-фикс.

**Деплой-памятка:** prod `root@188.245.42.4:/root/planner-v2-src/planner-v2`, rsync `frontend/` → `docker compose -f docker-compose.prod.yml build && up -d`. Health: `curl https://planner.188.245.42.4.nip.io/api/health` → 200. Гейт: `cd planner-v2/frontend && npm run lint:tokens && npm run test && npm run e2e && npx tsc --noEmit`.
