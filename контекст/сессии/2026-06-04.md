# Сессия 2026-06-04

## Цель сессии
Полировка экрана **Сегодня** по живым правкам владельца (визуал-гейт в Telegram).

## Сделано (всё задеплоено на prod + подтверждено бандлом)
**Деплой #1 (commit 7b76742, bundle index-sN5_el45.js):**
- Шапка «Сегодня» (заголовок + дата + счётчик) закреплена sticky (`.today-head`, onyx-фон, нижний бордер).
- Таймлайн стартует с **05:00** (`START_HOUR=5`, рендер 05:00→23:00). Edge: задача раньше 5:00 → сетка авто-растягивается вниз к её часу. Now-линия/блоки/автоскролл смещены на offset; now-линия прячется если время < старта.

**Деплой #2 (commit ba577ad, bundle index-Do3suDeU.js):** — 4 правки по фидбеку:
1. **Inbox-карта убрана из шапки** → Inbox остаётся в шторке слева («Входящие»). Шапка тоньше (padding s4/s2, hero margin 0) → таймлайн получил место.
2. **Тап по блоку на таймлайне → открывает TaskDetail** (`onOpen` проброшен Today→DayTimeline; закрыт долг «DayTimeline-блок onOpen не проброшен»).
3. **Чекбокс слева в cal-block** (`.cal-cb`) — только он отмечает done.
4. **Done-задача остаётся на таймлайне** (затемнена/зачёркнута), не пропадает.

## Файлы
- `frontend/src/screens/Today.tsx` · `components/DayTimeline.tsx` · `theme.css` · `App.tsx`.

## Деплой-факты (ВАЖНО — память противоречила)
- **Prod-каталог на VPS = `/root/planner-v2-src`** (НЕ `/root/personal-planner`!). Ветка `planner-v2-phase0`, remote `github.com-planner:logovikalexandr-spec/personal-planner`.
- Процедура: локально commit+push origin `planner-v2-phase0` → `ssh root@188.245.42.4` → `cd /root/planner-v2-src && git pull --ff-only` → `cd planner-v2 && docker compose -f docker-compose.prod.yml up -d --build`.
- Фронт собирается ВНУТРИ образа (Dockerfile multi-stage node→build→COPY dist; planner-api отдаёт `MINI_APP_DIST_DIR`). Коммитить dist НЕ надо.
- Публичный URL: `https://planner.188.245.42.4.nip.io/app/`. Проверка = 200 + сверка bundle-хэша с локальным build.
- `/root/personal-planner` на VPS = ДРУГОЙ репо (main, planner-bot v1 inbox) — не путать.

## Открыто / баги (с прошлой сессии, не трогали)
- Мерцание первого клика по табу (с 2026-05-29).
- Мелочь Волны 2: composer legacy recurrence:str; quick-add overlay не закрывается на смене таба; Tasks.tsx dead-code error-state.
- UX/UI до уровня Ledger (приоритет владельца), Календарь-редизайн (= копия Today), бэклог фич (Списки-атомы, Habits, Tracking, Notion-sync).

## База мокапов проекта + аннотатор (вторая часть сессии)
Цель: вывести каждую разработанную страницу/фичу в отдельный HTML — «база проекта» для поштучной разработки.

**Сделано (агентская сеть, не задеплоено — это локальный design-инструмент, вне prod):**
- **24 автономных HTML-мокапа** в `personal-planner/база-проекта/` (`pages/` 7 + `features/` 17), отрендерены Workflow-сетью (24 параллельных агента читали реальный код экранов/компонентов + DESIGN.md). Каждый — телефон-фрейм 390px, onyx+ember токены, реальные RU-данные, без emoji, с матрицей состояний (main + empty/loading/error/edge альт-фреймы).
  - Страницы: P01 Сегодня · P02 Календарь · P03 Списки · P04 Цели · P05 Трекинг · P06 Входящие · P07 Задачи.
  - Фичи: F01 TaskComposer · F02 TaskDetail · F03 QuickAdd · F04 RecurrenceSheet · F05 RemindersSheet · F06 DateDurationSheet · F07 ProjectSheet · F08 ProjectMenu · F09 BatchBar · F10 Checklist · F11 DayTimeline · F12 ListViewSwipe · F13 ProjectTree · F14 BottomTabs · F15 Pickers · F16 AddSheet · F17 Drawer.
- **`index.html`** — галерея-база: сетка карточек с живым iframe-превью, на каждой кнопки «открыть» + «разметить».
- **`annotator.html`** — локальный инструмент разметки (HTML/JS, без бэка): обвести область рамкой на мокапе + написать комментарий, авто-нумерация, localStorage, экспорт в `razmetka-<num>.md` (буфер + скачивание). Цикл: разметка → экспорт → правки по пометкам.

**Граба:** случайный `/ship` в середине прервал сессию и убил фоновый Workflow (все агенты получили `[Request interrupted by user]`); перезапуск с того же scriptPath добил 24/24. Вывод: interrupt сессии = смерть фонового workflow.

**Флаг (расхождение памяти):** `planner-v2/CLAUDE.md` §6 ФАКТЫ говорит «деплой = rsync, git remote нет» — УСТАРЕЛО. По deploy-фактам этой сессии: remote есть (`github.com-planner:...`), prod = `/root/planner-v2-src`, деплой = git pull + compose build. CLAUDE.md §6 надо поправить.

## Финализация мокапов база-проекта (design-review рубрика + agent-network)
Цель владельца: довести все мокапы до 100% как финальный продукт-дизайн. Чат поделён: **дизайн/HTML — этот чат, код — отдельный чат** (промпт-хендофф).

**IA эволюционировала (в мокапах):**
- Табы: Сегодня · Календарь · **Обзор** (дашборд + Гант) · Цели · Трекинг. «Списки» убран (проекты → шторка). Иконка Обзора = 2x2.
- **Проекты в шторке** (F17): emoji-иконки разрешены, цвет = **свободный селектор** (спектр, любой hex), свайп = [Изменить][Удалить] (пин убран), удаление → задачи во «Входящие».
- **P03 «Списки» → «Обзор»** (Сводка + Гант). **F08 ProjectMenu, F13 ProjectTree удалены**. **F18 Project Picker** добавлен. Итог: 7 страниц + 16 фич = 23 мокапа.
- Today: главный = таймлайн (Apple-стиль, all-day+FAB закреплены), кнопка **«Задачи»** (вместо AI) → гибрид-список. **annotator.html** — разметка мокапов.

**Канон цвета (КЛЮЧЕВОЕ):** фон задачи = тинт ЦВЕТА ПРОЕКТА (rgba 0.13), полоса 3px = ПРИОРИТЕТ (high=danger/med=warning/low=ember/none=нет). ember = только акцент, НЕ цвет проекта. Палитра: ZIMA #3FB68B · Здоровье #5B8DEF · Финансы #C9A24B · Планировщик #9B8CCB · Цели #9B6BE0 · Недвижимость #4FB0A8 · Духовность #C77DA8.

**Прогон:** аудит-сеть (23 агента vs DESIGN.md+решения) → консолидация → глобальные sed-фиксы → фан-аут финализации (23+3 агента), ~1.95M токенов. Итог: **23/23 к канону**, свип чистый (Списки/system-ui/--red-amber/#000000/Inter/«Won't Do» = 0). Унифицированы чекбокс 22px+steel, токены --danger/--warning, on-accent #1a1209, состояния empty(1 primary)/loading(скелетоны)/error. Баги F10 overflow, F18 статусбар.

**Граба:** агенты со `schema` иногда «completed without StructuredOutput» (9/23 не вернули отчёт, но правили файлы; F16/F17/F18 не тронули — перезапуск без schema). Вывод: для fix-фан-аута надёжнее без обязательной schema.

**Хвосты:** (1) `planner-v2/CLAUDE.md` §6 устарел — таб «Списки»(→Обзор) + «rsync/remote нет»(→git pull+compose). (2) Промпт код-чата пересобрать с финальными решениями.

## ── HANDOFF ──
- **Дизайн-трек (этот чат):** `база-проекта/` финализирована 23/23 по канону, референс для кода. Разметка — annotator.html.
- **Где встали (2 трека):**
  1. Экран Сегодня отполирован (sticky-шапка, таймлайн с 05:00, чекбокс+открытие детали, Inbox в шторке), оба деплоя живы, ждут финального визуал-ОК владельца в Telegram.
  2. Создана `база-проекта/` — 24 мокапа + галерея + аннотатор (локально, вне prod). Готова к поштучной разработке через разметку.
- **⭐ Следующий шаг (попросил владелец, СЛЕД. сессия):** inline-создание задачи **в тапнутой ячейке** (Apple-стиль: тап/протяжка по пустой сетке → блок растягивается на месте, инлайн-ввод там же), вместо bottom-sheet композера по тапу часа. Спроектировать флоу+мокап → сборка.
- **Вечер 2026-06-04 (ЗАДЕПЛОЕНО, 8 деплоев, бандл index-Bio7lpyg.js):** Today v2 по мокапу — 2 вида (timeline/tasks)+FAB-pill «Задачи», AI убран, цвет=тинт проекта+кант приоритет, backend scope overdue (105 pytest), жесты блоков (ресайз края+перенос long-press, снап 15мин на отпускании, глушитель скролла), project picker=сворачиваемое дерево, запрет zoom+выделения текста, фиксы скачков (оптимистичное время+firstRef, сетка всегда, scroll-lock оверлеев). Полный разбор — `memory/project-state.md`.
- **Не путать:** prod = `/root/planner-v2-src` (не personal-planner); ветка planner-v2-phase0; деплой = git pull + compose build (НЕ rsync). `база-проекта/` — design-инструмент, в prod не идёт.
