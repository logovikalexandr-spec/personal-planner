# Project State — Personal Planner

> Где остановились. Апдейтить на `/wrapup`.

## Последнее обновление: 2026-06-20 ВЕЧЕР (ГЛУБОКИЙ АУДИТ реального app — 4 дефекта, процесс перестроен)

**Триггер:** владелец прислал скрин реального Telegram — список «Все» в хлам (кнопки свайпа просвечивали поверх текста). Мой preview-свип показывал «чисто» → доказал: **аудит надо вести по РЕАЛЬНОМУ app со всеми входами, не по дефолтным табам/мокам.**

**РЕШЕНИЕ ПРОЦЕССА (закон):** `решения/единый-источник-компонент-как-мокап.md` — утверждаем живой React-компонент (preview), HTML-мокапы заморожены как референс. Двойная реализация = корень дрейфа, вырезан. TEAM.md Фаза1 + CLAUDE.md hard-rule 2 обновлены.

**4 дефекта найдено+починено+ЗАДЕПЛОЕНО (single build по каждому):**
1. **Легенда Ганта** — серые круги поверх свотчей (класс `sw` коллизил с глобальным toggle `.sw::after`). Фикс `sw→gsw`. (`index-QOfrClvc`)
2. **Просвет свайпа в списках** (скрин владельца) — `TaskItem` фон `${color}22` (13% alpha) просвечивал слоты Дата/В список/Удалить. Фикс: тинт слоем поверх opaque `var(--surface)`. (`index-DX6XU5jY`)
3. **Error-boundary** — любой краш компонента = белый/чёрный экран на весь app (боль владельца). Добавлен `ErrorBoundary` на корне → плашка «Что-то сломалось». + guards `TaskDetail` (subtasks/reminders/checkitems `?? []`). (`index-DsLHs9oV`)
4. **HeatRow guard** (`levels ?? []`) + полнота `preview-app-mock` (мок `/api/stages`, `heat7`, ErrorBoundary-обёртка). (`index-6paSfzRe`, прод live)

**Проверено живой навигацией (preview-app-mock):** все 5 табов рендерят + смарт-список «Все» + тап карточки→DETAIL (данные подтягиваются: Дата/Приоритет/Список/Вклад/Чеклист/Подзадачи). Краши DETAIL/HeatRow в preview были **mock-дырами** (бэк отдаёт массивы по умолчанию — подтверждено схемами TaskDetailOut/HabitOut), реальный app цел; guards+boundary = доп.защита.
**Грабли:** [[learnings]] «preview-моки врут». Скрины аудита: `planner-v2/frontend/design/audit-2026-06-20/`.

**ОСТАЛОСЬ в глубоком аудите (todo):** остальные смарт-списки (Сегодня/Завтра/7дней/Входящие/План) + проект-фильтр + ⋯-меню проекта · свайп-жест живьём (реальное действие) + create-флоу FAB + шиты/пикеры · INBOX-экран · DETAIL под-флоу (этап/повтор/напоминание-шиты) · проводка на реальный бэк (формы данных, не мок). Метод: гонять preview-app-mock по ВСЕЙ матрице входов, чинить, деплоить.

---

## Последнее обновление: 2026-06-20 (ФИНАЛ: T3+T4 слиты → ВСЕ 5 табов на проде, single build)

**Сделано:** домержены последние 2 блока в `planner-v2-phase0` → один build → деплой.
- `merge feat/t3-gantt` (17ce259) + `merge feat/t4-goals` (1a40dad). Конфликт ТОЛЬКО `theme.css` (оба аппендят секции в хвост от базы d934e7b) — резолв = оставить обе секции (T2→T3→T4), 0 потерь. Gantt.tsx/Goals.tsx — без конфликтов (per-screen файлы).
- **Гейт зелёный:** lint:tokens PASS · tsc 0 · vitest 66 · vite build чист · **e2e 104 passed / 12 skipped / exit 0** (T3+T4 функц.+токен-смоук на chromium+webkit; 12 skip = визуал-baseline T3/T4, эталон утверждает владелец).
- **ЗАДЕПЛОЕНО:** frontend-only rsync → `docker compose build && up`. Бэк НЕ трогал (прод alembic head `b9c0d1e2f3a4` = downstream от Форк0/impact/habits/stages → бэк полный, все эндпоинты живые). Прод-бандл `index-Cq_chkmP.js` (= локальный build-хэш), health 200, маркеры g-seg(T3)/t4-card/t4-pulse в живом бандле.
- **Итог: 5 табов на проде** — Задачи · Календарь · Гант · Цели · Привычки. Один build, без клоббера.

**ВИЗУАЛ-СВИП всех 5 вкладок (chrome-devtools, preview↔мокап) — сделал САМ, не перекладывал на владельца:**
- **Найден+починен реальный баг T3:** легенда Ганта рисовала серые круги поверх свотчей (класс `sw` коллизил с глобальным toggle `.sw`, его `::after`-knob). Фикс `sw→gsw` (1ba067b), задеплоен (бандл `index-QOfrClvc.js`, `gsw` в живом бандле, health 200). Гейт это пропустил (функц-e2e на testid + token-lint слепы к CSS-коллизиям) → грабли в [[learnings]].
- T1/T2/T4/T5 — визуально чисты, совпали с мокапами, сломов нет.
- Скрины аудита: `planner-v2/frontend/design/audit-2026-06-20/`.

**ОСТАЛОСЬ (только человек/владелец):**
1. **Финальный визуал-гейт в Telegram** по Гант+Цели (открыть Mini App, глазами под мокап T3a/b + T4-celi).
2. **Baseline-утверждение** (агент НЕ трогает): после визуал-ОК владелец сам `npx playwright test T3-gantt T4-celi --update-snapshots --project=chromium` → закоммитить эталоны (тогда 12 skip станут зелёными визуал-тестами).
3. Старые TODO актуальны: per-worktree порты vite; стейл T1 baseline; error-boundary; pinch-zoom/драг Волна-2 T3 (в COVERAGE-DEFER).

**Worktree-ветки** feat/t3-gantt/feat/t4-goals слиты — можно `git worktree remove planner-wt-t3 planner-wt-t4` (по слову владельца).

---

## Последнее обновление: 2026-06-15 (КООРДИНАТОР: T1-доводка + T2 слит → ОБА на проде; T3/T4 в работе)

**Прод:** `index-DzivQVfq.js`, health 200. Содержит **T1 (мои правки) + T2 Календарь**. Полный лог — [сессия 2026-06-15](../контекст/сессии/2026-06-15.md).

**Координатор задеплоил (ветка `planner-v2-phase0`):**
- Пикер «Дата/время» (`Task.end_date` миграция `b9c0d1e2f3a4`, рерайт DateSheet, многодневный=чип) `d934e7b`.
- Фиксы «Тоо»: now-линия `pointer-events:none` + ручка `align-self:stretch`+`touch-action:none` (баг [[planner-now-line-pointer-bug]]).
- Создание задачи только long-press (не тап); свайп-день+подсказка убраны.
- Короткий блок ≤30мин — заголовок по центру `0bf4c7f`.
- Деталь: действия в **⋯ меню** (не нижний бар), «Won't Do»→«Не буду делать», «Сабтаски»→«Подзадачи» `1b47965`/`9cb29aa`.
- Ручки ресайза = **кружки 9px accent** по диагонали (Apple) `ecb494e`.
- Список (Today tasks): **«Просрочено» под шторку + группы по проекту** (`lib/groupByProject`+vitest) + «Без времени·N» `6ec13d8`.
- **merge `feat/t2-calendar`→phase0** `3b7040e` (T2 Календарь Неделя/Месяц/Лента). Гейт tsc·lint·vitest 53.

**ПАРАЛЛЕЛЬ (worktree-изоляция, 4 блока):** T1=координатор(phase0,✅), T2=`planner-wt-t2`/`feat/t2-calendar`(✅ слит), T3=`planner-wt-t3`/`feat/t3-gantt`(⏳), T4=`planner-wt-t4`/`feat/t4-goals`(⏳).
**ПРАВИЛО ДЕПЛОЯ:** прод один → воркеры НЕ деплоят (только коммит в ветку) → координатор `merge feat/*→phase0`+деплой. Иначе затирают друг друга.
**ГОЧА:** worktree делят порт vite 5173 → playwright тестит чужое дерево; координатор верифает на изолир. порту (`vite --port 5266`+standalone).
**TODO:** домержить T3/T4 когда сдадут; per-worktree порты; 3 стейл visual-baseline (T1-today/full) ждут владельца `--update-snapshots`.

---

## Последнее обновление: 2026-06-14 ночь (T2 КАЛЕНДАРЬ собран — worktree planner-wt-t2, НЕ задеплоен)
**Блок A (ветка `feat/t2-calendar`, worktree `planner-wt-t2`).** Календарь был копией Today (только день). Собраны **3 вида под мокап** T2a/b/c:
- **Неделя** (`CalendarWeek.tsx`, новый): 7 колонок, окно 8–21 (`weekLayout`), таймблоки (фон=проект-тинт, кант=приоритет), now-line на сегодня, веха-флажки на числах, каскад наложений (G2 через `layoutColumns`), полоска «весь день» (C1), лоток «Без даты» (A8, свёрнут→раскрытие).
- **Месяц** (`CalendarMonth.tsx`, дособран): точки-задачи (макс3 +N), флажки-вехи, сегодня-кольцо, выбор дня→**панель дня под сеткой** (flow 10), сводка вех месяца (mstrip), легенда проектов.
- **Лента** (`CalendarAgenda.tsx`, дособран): просрочка закреплена сверху, группы Сегодня/Завтра/дни, **веха-строки** (без чекбокса), метка этапа, high→красный бейдж «важно», дни «свободно».
- **Контейнер** `Calendar.tsx` переписан: сегмент Неделя/Месяц/Лента + диапазон-нав ‹›+Сегодня + состояния loading/error(Повторить)/empty + per-view fetch (`getTasksRange`+`getMilestones`; undated через getTasks all; overdue через getTasks overdue). Деталь/композер — self-contained (App не трогал).
- **Общие файлы — только дописал:** `theme.css` (+секция cal2/cw-/cm-/ca-, токен-чистая, только `var(--c)` рантайм), `projectColor.ts` (+`priorityColor`), `weekLayout.ts` (+`nowLineTop`). `types.ts`/`api.ts` (CalendarView/getTasksRange/getMilestones) уже были (Форк B).
- **ГЕЙТ ЗЕЛЁНЫЙ:** lint:tokens PASS · 49 vitest (tsc 0) · **34 e2e** (`T2-calendar.spec.ts`, chromium+webkit, функц.+токен-смоук) · coverage **T2a 8/8 · T2b 5/5 · T2c 8/8 = 0 дыр**. Preview `preview-t2-mock.tsx`/`.html` (stateful, ?view=month/agenda · ?state=empty/error).
- **✅ ЗАДЕПЛОЕНО (2026-06-15, по слову владельца «давай деплоить»):** rsync frontend→`/root/planner-v2-src` + docker build/up. Бандл `index-CjPirXXe.js`, health 200, T2-маркеры (cal-week/cw-blk) в живом бандле. ПРЕД-ЧЕК: прод был чистый base (Gantt-стаб/Goals/Calendar day-only) — T3/T4 НЕ задеплоены, мой rsync = base+Calendar, клоббера нет. Финальный Telegram визуал-гейт владельца ещё нужен. **Baseline 3 chromium owner-апрув ЗАКОММИЧЕН** (15d2adf), gate locked.
- **⚠️ T3/T4 ещё НЕ на проде** (ветки feat/t3-gantt a5fb1f8, feat/t4-goals 1d1dcac готовы). Их деплой = координатор: merge всех в одну ветку → single build (отдельные worktree rsync клобберят друг друга, т.к. каждый = base+свой блок).
- **COVERAGE-DEFER (явно):** драг таймблока (flow 5) + драг из лотка на сетку (flow 8) — нужен pointer-харнес+бэк-PATCH; различие дедлайн/блокер бейджа — нужен флаг типа в модели (сейчас оба = high «важно»).
- **Инфра-нюанс:** в worktree нет `node_modules` → симлинк на `personal-planner/.../node_modules`. Стоял чужой vite на :5173 (reuseExistingServer отдавал код main → e2e тестил не тот код) — убил, поднял свой.

## Последнее обновление: 2026-06-14 вечер (creation-flow добит · datetime-дизайн готов · 2 задачи на новую сессию)

**T1 «создание задачи» — ДОВЕДЕНО (серия фиксов, все на проде):** «+» = ЕДИНСТВЕННОЕ быстрое поле «Быстрая задача» (TaskComposer-из-FAB удалён полностью). Чипы дата/проект/приоритет/тег/напоминание — рабочие, пикеры ИНЛАЙН (DateSheet/PriorityPicker/ProjectPickerSheet/TagPickerSheet) поверх оверлея (z-index шитов 70). «+» открывает БЕЗ клавиатуры (фокус на FAB). Шиты позиционируются из `visualViewport` (Sheet.tsx useLayoutEffect, без CSS-транзишна — не прыгают). Бар «Готово» + quick-overlay прижаты к низу через `lib/viewportAnchor.useBottomAnchor`. Тап ячейки часа → блок на НАЧАЛО часа (floor, не :30). Хинт пустого дня убран. 05:00 не режется (cal-grid margin). Быстрые-чипы даты (Сегодня/Завтра) убраны из DateSheet. Мокап-синк: `база-проекта-v3/pages/T1-quickadd.html`.

**СЛЕД. СЕССИЯ — 2 задачи (код):**
1. **Переделка «Длительность» → пикер «Дата/время»** (TickTick старт→дедлайн). Дизайн УТВЕРЖДЁН владельцем. Мокап `база-проекта-v3/pages/T1-datetime.html` (A одиночный день, B многодневный). Спека `planner-v2/docs/superpowers/specs/2026-06-14-datetime-picker-rework-design.md` (коммит 3aaf143). Суть: убрать вкладки Дата/Длительность → один экран старт(дата+время)→дедлайн(дата+время)+heat-календарь+весь-день+длительность-метка. **Бэк: +`Task.end_date` (миграция)** для многодневного. Многодневные спан-бары на таймлайне = follow-up.
2. **БАГ drag наложенных блоков:** блок в колонке 0 двигается long-press'ом, блок в колонке 1 (наложение по времени) — НЕ двигается (хотя resize краёв работает). Move-хендлеры (`onBodyDown`/`onDragMove` в DayTimeline.tsx) идентичны для обеих колонок — корень не виден чтением, подозрение на гонку pointer-capture/ре-рендер. **Разобрать с репро-харнесом** (2 наложенных блока), НЕ вслепую.

---

## Последнее обновление: 2026-06-14 (T1 гейт переподтверждён · T4 НАЧАТ → сохранён WIP)

**T1 «Задачи» — ЗАКРЫТ, гейт зелёный** (переподтверждено этой сессией): lint:tokens PASS · 35 vitest · tsc 0 · 52 e2e · coverage T1 = 7/7 ecode, 0 дыр.

**2 фикса владельца по живому Telegram ЗАДЕПЛОЕНЫ** (бандл `index-CyzGN8wN.js`, health 200):
- `ed8a073` убран хинт пустого дня («План на день пуст…») — пустой день = чистая сетка.
- `44a3a7d` первый лейбл `05:00` не режется липкой шапкой: `.cal-grid margin-top var(--s3)` (margin двигает rect.top целиком → жест создания не ломается).
- Заодно деплой починил застрявший на проде старый нав (был «Сегодня·Списки», стал «Задачи·Гант» — был кэш/старый бандл).
- `7f63e53` мёртвая зона long-press создания (баг «зажал 7:00 → черновик 7:30»). Корень ЗАМЕРОМ: `onHourMove` двигал блок на любой дрейф пальца, ~28px удержания → +30мин (snap15). Фикс `MOVE_THRESH=16` (margin-top НЕ виноват — гридлайны точны). Бандл `index-C2mV54YF.js`. UX-форк открыт: дрейф >16px всё ещё двигает (фича drag-to-position) — если владельцу мешает, опция = пинить на точке нажатия всегда.
- `d77857a` тап по ячейке часа → блок НА НАЧАЛО часа (тап в середину 07:00 давал 07:30; floor к часу для тапа, snap15 для драга). Подтверждён владельцем «всё работает».
- `5ba2d93` бар «Готово» — JS-позиция по visualViewport (top-якорь): `position:fixed+bottom` глючил в TG iOS (на верхних слотах scroll≈0 улетал вверх-вправо). Теперь стабильно низ-право над клавой на всех слотах. CSS bottom:0 = fallback. Бандл `index-BBGpYvkN.js`.
- ⚠️ **3 chromium визуал-baseline сдвинулись** (T1-today happy+empty, T1-full happy) — функ-тесты зелёные (49), baseline красные ОЖИДАЕМО. Владелец после визуал-ОК в Telegram переобновляет: `npx playwright test T1-today T1-full --update-snapshots --project=chromium`.

**T4 «Цели» — НАЧАТ, остановлен по слову владельца, СОХРАНЁН WIP `35a10f1`** (ветка planner-v2-phase0):
- ✅ Фронт Форк0 проведён: `types.ts` (Project AI-поля success_probability/target_date/ai_notes/weeks_left + тип AiNote) · `api.ts` (getStages, updateProjectAI — write-API, ZERO-AFK).
- ✅ `Goals.tsx`: заглушка → T4 список (пульс A1 + карточки A2-A7 {имя→деталь·кольцо%шанс·полоски этапов·веха·AI-заметка} + новый A8). Пульс/статус «в графике/отстаёт» — derive client-side (отстаёт = последняя ai_note risk ИЛИ этап late). tsc зелёный; app-nav.spec НЕ задевает (Goals в нав-тесте не открывается).
- ⏳ НЕ готово (продолжить отсюда по [плейбуку](../personal-planner/контекст/конвейер-плейбук.md)): T4-стили в `theme.css` (theme-токены, project.color через `--c`; мокап-классы t4-pulse/t4-card/t4-ring/t4-stages/t4-ai/t4-newbtn) → preview-t4-mock.tsx+html (stateful) → визуал-сверка vs `база-проекта-v3/pages/T4-celi.html` → спека `tests-e2e/T4-celi.spec.ts` (эталон T1) → coverage → деплой.
- 🔧 Бэк-пробелы для T4d ДЕТАЛИ (потом, доп-поля): недельная дельта %шанс (нет истории) + структурный разбор B1-B5 (ai_notes плоский список). T4-СПИСОК этих пробелов НЕ требует.

### T3 «Гант» Волна 1 — СОБРАНО, гейт зелёный, ДЕПЛОЙ ОТЛОЖЕН на след. сессию (2026-06-15)
**Где:** worktree `planner-wt-t3`, ветка `feat/t3-gantt`, коммит `a5fb1f8` (от базы `d934e7b`). Read-only PM-вид (T3a Все + T3b По проекту) по мокапам T3a/T3b + flows + состояния.
- ✅ `lib/ganttLayout.ts`(+test, 13 vitest): чистая px-раскладка — окно/колонки day·week·month, бар inclusive+min-1д, CPM-критпуть по длительности (подсветка только незакрытой части), конфликт-зависимостей.
- ✅ `screens/Gantt.tsx`: 2 режима (Все=строка-проект · Проект=этапы+задачи-мини-бары под текущим), зум День/Нед/Мес, h-скролл+кнопка «Сегодня», линия сегодня, late-«!», вехи-ромбы (flag=дедлайн/target_date, дедуп overlap), стрелки зависимостей+критпуть-обводка, шит детали этапа, поповер вехи, 5 состояний. Данные: getProjects+getStages фан-аут (как T4)+getTasks. ZERO-AFK, бэк не трогал.
- ✅ `theme.css` секция `.t3` (app-токены, project.color через `--c`) — ДОПИСАНО аддитивно. `preview-t3-mock.tsx`+`.html` (?state=empty/nostages/error). `tests-e2e/T3-gantt.spec.ts`.
- ✅ Гейт: lint:tokens PASS · vitest 61 · tsc 0 · e2e 38 passed (chromium+webkit) · coverage T3a 7/7·T3b 8/8·0 дыр. Визуал сверен глазами (chrome-devtools) — совпал с мокапами.

**ЧТО СДЕЛАТЬ НА СЛЕД. СЕССИИ (деплой T3):**
1. ⚠️ **Визуал-baseline 3 шт КРАСНЫЕ** (эталона нет — это гейт). Владелец смотрит в Telegram → утверждает САМ: `npx playwright test T3-gantt --update-snapshots --project=chromium` (агент эталон НЕ трогает).
2. **Деплой = ФРОНТ-ONLY rsync** (выбор владельца, низкий риск, без миграций — T3 бэк не менял). Прод-бэк УЖЕ знает `/api/stages`+`/api/milestones` (проверено: 401, не 404). Команда из плейбука: rsync `frontend/` из wt-t3 → `/root/planner-v2-src/planner-v2/frontend/` → `docker compose -f docker-compose.prod.yml build && up -d` → health 200.
3. ⚠️ **SERIAL DEPLOY — прод один.** Убедиться, что T2/T4-чаты НЕ билдят прод одновременно (два build+up = поломка). Координировать.
4. **Топология (важно):** прод `planner-v2-phase0 @ 4dc313d` = на **36 коммитов ПОЗАДИ** базы форка `d934e7b`. Фронт-only rsync из wt-t3 поднимет прод-фронт до d934e7b+T3 (вкл. нав-бар+T1-финал) — T2/T4 НЕ затрёт (их веток на проде нет). Альтернатива (если решат полный): merge feat/t3-gantt→phase0 + git pull + alembic-миграции + build (больше blast-radius). Remote: `git@github.com:logovikalexandr-spec/personal-planner.git`.

**Отложено в Волну 2 T3** (в COVERAGE-DEFER спеки): драг тела=сдвиг дат · драг края=ресайз · правка дат из шита · удаление этапа · конфликт-флаг при драге · pinch-zoom · открытие DETAIL из мини-бара (App-уровень) · функц-тест h-скролла/кнопки «Сегодня». Бэк для Волны-2 драга готов (`PUT /api/stages/{id}` поддерживает start/end_date).

---

## Последнее обновление: 2026-06-14 (КОНВЕЙЕР ВЕРНОСТИ v2 — Playwright, prevention-first)

**Зачем:** боль «макет есть, код выходит неточным». Старый план (греп+инертный YAML+chrome-devtools-MCP+пиксель-диф мокап↔рендер) после профи-аудита признан переизобретением visual-regression/BDD/tokens + анти-паттерны → переписан. Дизайн: `контекст/спеки/2026-06-14-конвейер-v2-playwright-design.md`. План: `контекст/планы/2026-06-14-gate1-playwright-v2.md`. v1-доки заархивированы ⛔.

**СДЕЛАНО (4 коммита на planner-v2-phase0, всё зелёное):**
- `5ca1571` Слой предотвращения: `tools/check-tokens.sh` (гард сырого hex+битых var, zero-dep, `npm run lint:tokens`). +3 токена. **+5 тихих багов цвета пофикшено** (var(--onyx/red/steel/hairline)+дубли).
- `61acfd5` Playwright-скелет: config 390×844 chromium+webkit, `_telegram.ts` стаб, smoke. **updateSnapshots:"none"** (агент не трогает baseline).
- `6781013` Гейт на T1 «Сегодня» (golden example): `tests-e2e/T1-today.spec.ts` 14 тестов (визуал-baseline+токен-смоук+структура+empty), `coverage.sh` (ecode→тест/defer, PASS 1 покрыт A6·6 отложено·0 дыр), data-testid в DayTimeline, TEAM.md перевшит на авто-гейт. **baseline утв. владельцем визуально** (happy+empty).
- `53e0602` Дизайн-доки конвейера.

**«Сегодня» ДОБИТ до 100%** (`951d925`): полноэкранный `preview-t1-full-mock` (Today+хром, stateful fetch-мок) + `T1-full.spec` (A1 бургер·A2 датапикер·A3 jump·A4 саммари·A5 all-day«+N»·draft·**persist после refresh**·**drag snap-15**). Today.tsx размечен data-testid. coverage.sh принимает неск. spec. Покрытие T1 = 6/7 ecode (A7 FAB=App-уровень отложен), 0 дыр. Визуал-baseline детерминирован через `page.clock`. Гейты зелёные: lint·35 vitest·34 e2e(×2 браузера)·tsc·coverage.

**T1 ДОБИТ ПОЛНОСТЬЮ + НАВ-БАР + ДЕПЛОЙ** (сессия 2026-06-14-2, см. [сессию](../контекст/сессии/2026-06-14-2.md)):
- `1f7dd20` FAB-дрейф (пилюля «Задачи»→«Список»+иконка+позиция) + подсказка свайпа — залочено. T1 = 7/7 ecode.
- `bdeac65` **Нав-бар IA под мокап**: Задачи·Календарь·**Гант**·Цели·Привычки («Сегодня»→«Задачи», «Списки»-таб→Гант+стаб-экран; Списки в бургере). `app-nav.spec` на РЕАЛЬНОМ `<App/>` (preview-app-mock).
- `4fc72d5` Датапикер T1·B: бот-шит→**поповер**+квадраты-heat+1 кнопка. Залочено.
- `06d2060` **ЗАДЕПЛОЕНО на прод** `planner.188.245.42.4.nip.io/app/` (rsync+build+up, фронт-only, ledger не тронут). Bundle `index-Cch6KAhO.js` live. URL=**nip.io** (CLAUDE.md-факт sslip исправлен).
- Гейты: lint·35 vitest·52 e2e·tsc·coverage 7/7.

**КАК ПРОДОЛЖАТЬ (turnkey):** [конвейер-плейбук.md](../контекст/конвейер-плейбук.md) — рецепт «добить экран» + деплой. Бэклог: [аудит-дрейфа](../контекст/аудит-дрейфа-2026-06-14.md).

**СЛЕД (владелец: строить непостроенное по мокапу, по плейбуку):**
1. **T4 Цели** (заглушка→построить; бэк Форк0 готов; мокап T4-celi/T4d).
2. **T2 Календарь** Неделя/Месяц/Лента (сейчас вид дня; недели нет, только weekLayout-хелперы).
3. **T3 Гант** (стаб-таб стоит; мокап T3a/b).
4. Error boundary (кривое поле→белый экран).
5. Доаудит шторки/DETAIL/INBOX.
6. Инфра-TODO: авто-запуск гейта (pre-commit+CI) — сейчас ручной.

**НЕ переписывать:** T1+нав+общие компоненты корректны = эталон, копировать.

**ПРОВЕРКА гейта:** `cd planner-v2/frontend && npm run lint:tokens && npm run test && npm run e2e && bash tests-e2e/coverage.sh ../../база-проекта-v3/pages/T1-zadachi.html tests-e2e/T1-today.spec.ts`. Эталон обновляет ТОЛЬКО человек: `npx playwright test T1-today --update-snapshots --project=chromium`.

---

## Последнее обновление: 2026-06-11 ночь-3 (КОД-ФАЗА: impact + Форк E + Tracking + Ретро + метка этапа)

## ── ПРОГРЕСС 2026-06-12 (код-фаза, поэтапно по страницам V3) ──
**Деплой ночь-3 ЗАДЕПЛОЕН** (`9ef9fd8`): impact+Форк E+Tracking+Ретро+метка этапа на прод, миграции `→a7b8c9d0e1f2`, bundle `index-DA-4UJD0.js`, density-цепочка проверена.
**T1 «Задачи» ДОБИТА ДО МОКАПА + ЗАДЕПЛОЕНА** (`4dc313d`, bundle `index-CP5a8L3E.js`):
- G1 heat-датапикер (T1·B): бэк `day_density` (g/y/r, red=≥5 или прошлый-high; ZERO-AFK) + `GET /api/tasks/density` + DateJumpSheet заливка месяца + кольцо сегодня. 7 тестов.
- G2 all-day «+N ещё» (A1): 2 чипа + разворот.
- G4 состояния T1: error (треугольник+подзаголовок+Повторить), list-empty (smiley+«День свободен»+кнопка).
- Гейт 160 pytest·35 vitest·tsc0·build. Визуал-самопроверка `preview-t1-mock.html` пройдена (heat/+N/состояния скринами). ⚠️ финальный Telegram-гейт владельца ещё нужен.
**СЛЕД по страницам:** T2 Календарь (бэк Форк B готов: range+/api/milestones, фронт не собран) → T4 Цели (бэк Форк0) → T3 Гант (нужны drag-эндпоинты) → INBOX (suggested_stage_id+эвристика) → DETAIL-фронт (блоки Этап/Вклад в детали) → Tracking деталь-экраны.

## ── HANDOFF 2026-06-11 ночь-3 (прораб) → НОВЫЙ ЧАТ (продолжить код-фазу) ──
КОД-ФАЗА идёт на снимке `a0b245a` (ветка planner-v2-phase0, локальный). **12 коммитов, всё зелёное: 153 pytest · 35 vitest · tsc 0 · vite build. НЕ ЗАДЕПЛОЕНО.** Деплой = rsync `/root/planner-v2-src` + миграции — за владельцем (нужен Telegram визуал-гейт).

**СДЕЛАНО И ПРОВЕРЕНО СКРИНАМИ (само-проверка, финальный вкус-гейт владельца в Telegram ещё нужен):**
1. **Task.impact** (вклад%): поле+миграция `f1a2b3c4d5e6`, токен `NN%` в TaskItem/DayTimeline/деталь, `shouldShowImpact` (≥30+привязка). Формат `80%` (НЕ стрелка).
2. **Форк E backend+фронт** (привычки/метрики): 4 модели+миграция `a7b8c9d0e1f2`, сервис (toggle/сумма/**градиент heat_level 0-4**/стрик/метрика-замена), API `/api/habits|metrics|tracking/retro`. Tracking.tsx ПРОВЕДЁН на API (сегменты Привычки/Метрики/Ретро живые, шиты создания, удаление, loading/empty/error).
3. **Метка этапа A6**: `Task.stage` relationship → `stage_label`/`stage_status` в TaskOut; `PATCH stage_id`; фронт рисует «этап N» цветом статуса в таймлайне/строках.
4. **Ретро задач+привычек** (`week_review`): задачи по проектам+вклад+**просрочка списком реальных TaskItem-карточек**(шторка, красная рамка)+топ-задача; привычки недели+теги; метрики недели(дельта по good_direction); хайлайты(SVG молния/звезда/треугольник); заметка ассистента(ZERO-AFK). Скрины `frontend/design/retro-*.png`, `today-stage-impact.png`, `tracking-*.png`.

**ЧТО ОСТАЛОСЬ (новый чат, по приоритету владельца):**
- **Tracking деталь-экраны**: привычка(месяц-календарь+бэкфилл), метрика(график+лог замеров), count-степпер-шит. (бэк: backfill/measure уже есть.)
- **DETAIL фронт**: блок «Этап проекта»(сменить/убрать) + блок «Вклад в успех» В САМОЙ детали (сейчас impact-строка есть, stage-блок только метка в таймлайне). Мокап `DETAIL-flows`/`DETAIL-task`.
- **Фронт-экраны новых табов** (мокапы утв., React не собран): T2 Календарь(бэк готов Форк B: range+/api/milestones) · T4 Цели(бэк готов Форк 0: success%/stages/ai_notes) · T3 Гант(Stage есть, нужны drag-эндпоинты) · INBOX(нужно suggested_stage_id + эвристика).
- **Деплой** + миграции `f1a2b3c4d5e6`→`a7b8c9d0e1f2` (add-column/create-table, безопасно) + Telegram визуал-гейт.
- Мелочь: Ретро avg «/день» (=done/7); метрика-дельта недели (сейчас last-vs-prev).

**ПРАВИЛА код-фазы:** TDD, всё зелёное на чекпойнте, коммит. Визуал-проверка перед «готово» (скрин через mock-preview `frontend/preview-*.html` + chrome-devtools, либо владелец в Telegram). emoji = иконки проектов через пикер, семантика=SVG ([[feedback-emoji-icon-system]]). ZERO-AFK: бэк LLM не зовёт. Параллельные форки → git worktree-изоляция (урок a0b245a). Локальный превью: `cd planner-v2/frontend && npx vite --port 5174` → `/app/preview-tracking-mock.html`.

## ── HANDOFF 2026-06-11 ночь-2 (КОД-ФАЗА начата автономно: impact + Форк E backend) ──
## Последнее обновление (старое): 2026-06-11 ночь-2

## ── HANDOFF 2026-06-11 ночь-2 (прораб, автономный «кофеин»-прогон) → новый чат ──
Владелец ушёл AFK, попросил довести до конца. Вошёл в КОД-ФАЗУ на снимке `a0b245a`. TDD, всё зелёное, 5 коммитов (НЕ задеплоено — деплой нужен визуал-гейт владельца в Telegram).

**Сделано + проверено (evidence):** `150 pytest · 35 vitest · tsc 0 · vite build · 1 миграция-head`.
1. **Фича Task.impact** (спека `docs/superpowers/specs/2026-06-11-task-impact-vklad-design.md`, план `planner-v2/docs/superpowers/plans/2026-06-11-task-impact.md`):
   - Бэк: поле `Task.impact 0-100|null` в модели/TaskOut/TaskPatch/_TASK_FIELDS + миграция `f1a2b3c4d5e6`. Пишет Claude через PATCH (ZERO-AFK).
   - Фронт: `types.ts` impact, `lib/impact.ts shouldShowImpact` (порог 30 + привязка к проекту/цели) +vitest, рендер токена `NN%` в TaskItem/DayTimeline-мете + строка «Вклад в успех» в TaskDetail (показ всегда + null «обсуди с ассистентом»). Класс `.imp` в theme.css.
2. **Форк E backend** (привычки+метрики, ZERO-AFK — главный код-пробел закрыт):
   - 4 модели `habit/habit_entry/metric/metric_entry` + миграция `a7b8c9d0e1f2`.
   - Сервис `services/tracking.py`: check-toggle / count-СУММА дня, **градиент-зачёт `heat_level` 0-4** (по доле value/target), дневной стрик (+record), метрика=ЗАМЕНА(upsert), ретро недели. Чистые хелперы `habit_is_done`/`habit_heat_level` юнит-покрыты.
   - API `routes/tracking.py`: `/api/habits` CRUD + toggle/add/backfill, `/api/metrics` CRUD + measure, `/api/tracking/retro`. Зарегистрирован в app.py.
   - 14 тестов (10 сервис + 4 API).

3. **Tracking.tsx ПРОВОДКА на API готова** (коммиты 8dddfa1, f12d984): статик-хардкод заменён — fetch getHabits/getMetrics/getRetro, toggle/add/measure через API, **градиент-heatmap из heat7 бэка**, стрик/неделя/ретро с сервера, сегменты Привычки|Метрики|Ретро, шиты создания привычки/метрики, удаление (tg.showConfirm), loading/empty/error. **Визуал-самопроверка пройдена** (mock-preview `frontend/preview-tracking-mock.html` + headless-скрины `frontend/design/tracking-{live,metrics,retro}.png`) — совпадает с мокапом T5. ⚠️ финальный вкус-гейт владельца в Telegram ещё нужен. Полировка: Ретро-карточка чуть жмёт заголовок к цифрам. Деталь-экраны (месяц-календарь привычки, график метрики, бэкфилл по ячейке heatmap, count-степпер-шит) — НЕ сделаны, след. шаг.

**НЕ сделано (намеренно — нужен визуал-гейт владельца, не строил вслепую):**
- Tracking деталь-экраны (см. выше) — мелочь, можно автономно + скрин.
- Фронт-экраны T2/T3/T4/INBOX — тоже после визуал-гейтов.
- **Деплой** (rsync `/root/planner-v2-src`) — не делал, нужен твой запуск + Telegram-проверка. Миграции `f1a2b3c4d5e6`→`a7b8c9d0e1f2` накатить при деплое (round-trip на PG не гонял локально — add-column/create-table, безопасно).

**СЛЕД новый чат:** (a) визуал-гейт мокап-волны постранично ИЛИ (b) Tracking.tsx проводка на API под мокап T5 (+градиент heatmap) с визуал-проверкой ИЛИ (c) фронт-экраны T2/T3/T4/INBOX по утв. мокапам. Снимок a0b245a + 5 коммитов = база. ⚠️ параллельные код-форки → git worktree-изоляция (урок гонки a0b245a).

## Последнее обновление: 2026-06-11 ночь (МОКАП-ВОЛНА добита + фича «вклад в успех»)

## ── HANDOFF 2026-06-11 ночь (прораб) → новый чат ──
**ФАЗА = МОКАПЫ (код потом).** Владелец подтвердил: доделываем ВСЕ мокапы/флоу/фичи до конца, ПОТОМ код.

**Сделано этой сессией:**
1. **Верификация снимка `a0b245a`** (код-форки 0/A/B/E): 134 pytest + 30 vitest + tsc/build зелёные, shared-файлы целы (types/api/theme/models не затёрты), клоббера нет. Снимок достоверен. ⚠️ это КОД-фаза, отложена — сейчас мокапы.
2. **МОКАП-ВОЛНА добита (5 форков параллельно, раздельные файлы — гонок нет).** Каждая страница V3 доведена до глубины T1 (база+флоу+состояния):
   - T2 Календарь → `T2-flows.html`(10 флоу)+`T2-состояния.html`
   - T3 Гант → `T3-flows.html`(11)+`T3-состояния.html`
   - T4 Цели → `T4-flows.html`(8)+`T4-состояния.html`
   - DETAIL → `DETAIL-flows.html`(этап)+`DETAIL-состояния.html`
   - INBOX → `INBOX-flows.html`(9)+`INBOX-состояния.html`
   - T5 уже имел `T5-flows.html`(11) — подтверждён.
   Бриф: `контекст/forks-mockups-2026-06-11.md`. Галерея `база-проекта-v3/index.html` +секция «Флоу+состояния». Реестр обновлён (таблица комплектности).
3. **Фича «Вклад в успех» (брейншторм→спека→мокап).** Спека `docs/superpowers/specs/2026-06-11-task-impact-vklad-design.md`. Модель: `Task.impact 0–100|null`, пишет Claude в чате (ZERO-AFK, как success_probability). UI = моно-токен `80%` в мете строки рядом с «этап N» (НЕ цвет — канал занят кант=приоритет/фон=проект/мишень=этап). Показ: `impact≥30` И привязка к проекту/цели. Вплетён в T1 (таймлайн+список) + DETAIL (блок «Вклад в успех»+обоснование+состояние null). Варианты: `T-impact-варианты.html`, `T-impact-v2-каналы.html`. Владелец УТВЕРДИЛ.
   - **Парк (бэклог, НЕ сейчас):** вид «Фокус» (сортировка по вкладу) + Матрица Эйзенхауэра. Токен самодостаточен.

**Статус мокапов V3: ВСЕ 7 страниц = база+флоу+состояния готовы.** T1 утверждён ранее; T2/T3/T4/T5/DETAIL/INBOX + токен — владелец сказал «нравится» (общий ОК), формального постраничного «утверждаю» по каждой ещё не давал.

**СЛЕД (на выбор нового чата, решает владелец/прораб):**
- (a) Постраничный визуал-гейт оставшихся 6 (если нужен строгий контракт перед кодом) ИЛИ
- (b) Консистенси-полировка (токен в T2/T4 где есть мета — опц.) ИЛИ
- (c) **ВХОД В КОД-ФАЗУ:** вернуться к снимку `a0b245a`, доделать недоделанное по форкам (Форк E backend Habit/Metric — НЕ написан, только статик-вёрстка Tracking.tsx; см. блок ниже) + новое поле `Task.impact`. ⚠️ ПЕРЕД параллельным кодом — git worktree-изоляция на форк (урок гонки a0b245a). Деплой мокапов не нужен (просто файлы); деплой кода = rsync `/root/planner-v2-src`.

## ── HANDOFF 2026-06-11 вечер (прораб) → новый чат ──
**Где встали:** A/B/E отработали ФАЗУ 1 (контракты мокапов утверждены) + частичный код-черновик. ФАЗА 2 (полная сборка) у всех ещё открыта. Остальное (C/D/F/G) — новый чат.
- **Форк A (T1 Задачи):** контракт утверждён; код T1 преждевременно тронут (Today/DateJumpSheet/icons/theme/App). Фаза 2: собрать A1/A2(без тепла)/A3/A5/A7/A8/A9/D+состояния; A6-метка+B2-тепло после общих файлов.
- **Форк B (T2 Календарь):** 3 вида утверждены; БЭК готов (range-фильтр + `/api/milestones` across-projects, **134 pytest + 10 vitest зелёные**); ФРОНТ = черновик до утв.мокапов → **пересобрать под финал; CalendarWeek НЕ написан; theme.css cal-стили долг**. Месяц дорос до панель-дня, Лента — просрочка+вехи-строки (не только стиль — функция).
- **Форк E (T5 Привычки):** контракт `T5-habits.html` утверждён (Привычки+Метрики+12 флоу); по факту **полная сборка, НЕ перенос 1:1** (форк-док врал). Модель: **2 новые сущности / 2 таблицы** (привычка count=сумма · метрика=замена) — ПОКА в models.py НЕТ (Фаза 2: миграция + пересбор Tracking.tsx под контракт). Открыто: IA сегменты-vs-скролл, порог зачёта воды, суб-вид Ретро.
**КРИТИЧНО — гонка общих файлов случилась:** все форки 0/A/B/E писали в ОДНО дерево `planner-v2/` без коммитов. Прораб сделал **страховочный коммит `a0b245a`** (ветка planner-v2-phase0, локальный, НЕ задеплоен) — захватил всё (1976 строк, 30 файлов). Снимок = уже СОВМЕЩЁННЫЙ результат всех форков на диске (они накладывали правки друг на друга в общих файлах theme.css/App.tsx/types.ts/api.ts/schemas.py/models.py/icons.tsx/BottomTabs.tsx).
**ПЕРВЫЙ ШАГ нового чата — ВЕРИФИКАЦИЯ совмещения (не доверять, что наложение целое):**
1. `cd planner-v2` → pytest (ожидалось 126+ зелёных от Форка 0 + тесты B `test_forkb_calendar.py`) → vitest (`calDates.test.ts`/`weekLayout.test.ts`) → frontend build чистый.
2. Глазами свести общие файлы: не затёр ли один форк правки другого (особенно theme.css/types.ts/schemas.py — туда лезли и A, и B).
3. Доделать Форк E (Tracking) если не закончил.
4. Визуал-гейт в Telegram по утверждённым мокапам A/B/E → деплой rsync `/root/planner-v2-src`.
**ВОЛНА 2 (C Гант / D Цели / F DETAIL-этап / G INBOX):** ПЕРЕД стартом завести **git worktree-копии на форк** (изоляция), чтоб гонки не повторить. Все 4 сидят на модели Форка 0 (готова).
**Артефакты:** контракты в `база-проекта-v3/`, конвенция адресов мокапов `контекст/реестр-мокапов.md`, бриф форков `контекст/forks-2026-06-10.md` (Фаза1 мокап→утвердить, Фаза2 код), ZERO-AFK закон.

## ⭐ СЕССИЯ 2026-06-11 — Прораб-сводка: `контекст/сессии/2026-06-11.md` (читать первой)
- **Контракт T1 (V3 P01 «Задачи») УТВЕРЖДЁН** владельцем. Мокап `база-проекта-v3/pages/T1-zadachi.html` (4 фрейма) + `T1-состояния.html` + `T1-D-heat.html`. Решения A1–A9/B2/D зафиксированы в реестре.
- **Система навигации-бейджей: 33 мокапа размечены** (V2 P01–P07 + F01–F18, V3 T1/T2*/T3*/T4/T4d/T5/DETAIL/INBOX). Адрес = `<версия> <страница> <фрейм><элемент>` (напр. `V3 T1 A4`). Источник: **`контекст/реестр-мокапов.md`** (в MEMORY.md) — конвенция + карта пар V2↔V3 + кросс-рефы.
- **Принципы работы** (через них гоним): мокап-first→«утверждаю»→сборка · мокап=контракт+визуал-диф · протокол «✓ записал» · ZERO-AFK · варианты на мокапах. Полностью — в сессии 11.06.
- **СЛЕД (Фаза 2 сборки T1):** сверить shared-файлы после мержа Форка B → собрать A1/A2(без тепла)/A3/A5/A7/A8/A9/D+состояния в Today-файлах → A6-метка+B2-тепло после Форка B (shared). ⚠️ преждевременно тронут код T1 (Today.tsx/DateJumpSheet/icons/theme/App/schemas/types) + Форк B пишет те же shared-файлы (гонка, коммитов нет). Деплой этого репо = rsync `/root/planner-v2-src` (НЕ git — у форк-дерева remote нет).

---

## ⚡ ZERO-AFK ЗАКОН + ФОРК-ПЛАН (2026-06-10)
Полный бриф: `контекст/forks-2026-06-10.md`. Память: `Alexandr/memory/zero-afk-architecture.md`.
- **С 15 июня Anthropic делит биллинг:** human-in-loop=подписка, AFK(фон зовёт LLM)=платный кредит≈API×10. Александр хочет 0 API.
- **Закон планировщика:** бэкенд НЕ зовёт LLM. App=тупой трекер (FastAPI+PG). AI-поля (Project.success_probability/target_date/ai_notes + Stage) заполняет интерактивный Claude через write-API. «Петля 5мин»=on-demand, НЕ фон-демон. INBOX-подсказка=эвристика/чат, не live-LLM.
- **Форк-план: 8 форков по страницам v3, каждый в отдельном чате** (читает forks-2026-06-10.md → свою секцию). Форк0=фундамент модели (Stage/Milestone+Project AI-поля+Task.stage_id+write-API), ПЕРВЫМ и последовательно. Затем A(Задачи)/B(Календарь)/E(Привычки) параллельно безопасно; после 0 — C(Гант)/D(Цели)/F(DETAIL-этап)/G(INBOX). Общие файлы (theme.css/App.tsx/BottomTabs/models.py) строго последовательно.
- **TODO смежное:** проверить Ledger/ZIMA на скрытый AFK (cron app-логики без LLM = ОК; AFK только если фон зовёт Claude).


## ⭐ РЕСТРУКТУРИЗАЦИЯ: AI ПРОДЖЕКТ-МЕНЕДЖЕР (2026-06-09 сессия 2, brainstorm)
Полный разбор: `контекст/сессии/2026-06-09-2.md`. Спека: `docs/superpowers/specs/2026-06-09-product-audit-and-restructure.md`.
Продукт переосмыслен из todo в **AI проджект-менеджера / систему достижения целей** (оценка текущего 28/100).
- **Два слоя:** Claude Code = мозг (разбор проекта: вероятность%, срок, как ускорить, план по этапам), приложение = трекинг + быстрый захват. **Петля ~5мин двусторонняя** (статус назад → агент знает реальность).
- **5 табов:** Задачи (любой день+датапикер=слияние Сегодня+Календарь-день) · Календарь (гибрид Неделя+лоток/Месяц/Лента) · Гант (все/проект) · Цели (Обзор-пульс + проекты %успеха/срок/этапы/AI-заметки) · Привычки (+метрики/heatmap/ретро).
- **Модель +:** Проект{вероятность%,срок,AI-заметки}, Этап/Веха (новое), Задача→этап.
- **Мокапы:** `база-проекта-v3/` (галерея + 10 экранов, НЕ трогать V2). T1 «Задачи» финал (☰ бургер=шторка проектов/свайп◀▶=даты/датапикер/метка этапа нейтральная/FAB «Список»). TaskDetail + поле «Этап», Сабтаски→Подзадачи.
- **Правило:** рабочее НЕ переписываем (инлайн-создание/шторка→бургер/TaskDetail/свайпы/quick-add/жесты) — структура+достройка.
- **HANDOFF:** текущий чат доделывает ВСЮ логику таба «Задачи» (остальные страницы форкнуты владельцем в отдельные чаты). Дизайн-док North Star ещё не записан; мост+петля (фундамент) — спроектировать.

## INLINE-СОЗДАНИЕ В ЯЧЕЙКЕ + КОЛОНКИ + DELETE — ЗАДЕПЛОЕНО, владелец подтвердил (2026-06-09)
Полный разбор сессии: `контекст/сессии/2026-06-09.md`. Ветка planner-v2-phase0.
- **Inline-создание задач на таймлайне** (заменило bottom-sheet по тапу часа): тап=блок 1ч, long-press=двигать 1ч-блок+активировать карточку, драг сетки=нативный скролл, тап блока=деталь. Черновик с инлайн-инпутом (editing/saving/error), «Готово» над клавой, при черновике скрыты tabbar+fab. Длину тянешь за края (ресайз-грипы видны ТОЛЬКО у зажатого/активного блока).
- **Наложения = колонки (Apple)** — `lib/timelineLayout.ts layoutColumns` (greedy lane).
- **vitest заведён** на фронт (его не было): `lib/timelineLayout.test.ts` 13 юнит-тестов (чистые функции жеста/раскладки).
- **Delete задачи починен:** (1) `window.confirm` в TG WebView не работает → `tg.showConfirm`; (2) **бэкенд-роута DELETE /api/tasks/{id} НЕ БЫЛО** → реализован svc.delete_task (сабтаски каскадом) + роут 204 + тесты. **107 pytest.**
- Процесс: заведён ВИЗУАЛ-ФИДЕЛИТИ ГЕЙТ (мокап=контракт) в TEAM.md.
- **СЛЕД:** мокапы база-проекта (P01/F11) синхронизируются под новый функционал (фоновый агент 2026-06-09) — проверить. Дальше: Δ1 «Списки→Обзор» (код отстал от финального мокапа) ИЛИ новая волна (Цели/Трекинг) ИЛИ довести экраны до мокап-канона.
- Артефакты фичи: `docs/superpowers/specs/2026-06-09-inline-cell-create-deep-spec.md`, `plans/2026-06-09-inline-cell-create.md`, `design/inline-cell/*.html`.

## ЭКРАН СЕГОДНЯ v2 + ЖЕСТЫ ТАЙМЛАЙНА — ЗАДЕПЛОЕНО (2026-06-04 вечер, 8 деплоев, все live)
Большая сессия по утверждённому мокапу `база-проекта/pages/P01-segodnya.html`. Финальный бандл `index-Bio7lpyg.js`.

## ЭКРАН СЕГОДНЯ v2 + ЖЕСТЫ ТАЙМЛАЙНА — ЗАДЕПЛОЕНО (2026-06-04 вечер, 8 деплоев, все live)
Большая сессия по утверждённому мокапу `база-проекта/pages/P01-segodnya.html`. Финальный бандл `index-Bio7lpyg.js`.
- **Today = 2 вида внутри таба** (todayView в App): `timeline` (главный: таймлайн 05:00, чип «Сегодня»=прыжок к now, all-day чипы sticky в шапке) + `tasks` (Inbox-карта + inline QuickAdd + Просрочено + Без времени + сворачиваемое Закрыто; назад-чип «Таймлайн»). FAB-pill «Задачи» переключает. (commit c527346)
- **FAB:** AI убран везде (+ вырезана заглушка AI-копайлот из App). Вторичный FAB опционален (labeled pill).
- **Цвет задачи:** фон = тинт проекта (~13%, TaskItem+DayTimeline); кант 3px СТРОГО приоритет (none=без полосы), убран фолбэк канта на цвет проекта.
- **Backend:** `list_tasks` scope `overdue` (due_date<today, status todo/in_progress) + TDD-тест. **105 pytest зелёные.** Без миграций.
- **Жесты таймлайна** (DayTimeline, pointer events): ресайз за края (верх=старт/низ=конец) + перенос всего блока (long-press 220мс «поднять»). Снап 15 мин ТОЛЬКО на отпускании, во время драга — непрерывное следование за пальцем (иначе дёргалось). Глушитель скролла на жесте (non-passive touchmove preventDefault, как ProjectTree). (commits 500248d, ea9ac20)
- **Project picker** (карточка задачи→Проект): сворачиваемое дерево + chevron + счётчик поддерева (reuse flatten{expanded}/childCountMap/subtreeCountMap/IcoChevron), дефолт свёрнут, авто-раскрытие к выбранному. (commit c3e68dc)
- **Глобально:** запрет zoom (viewport user-scalable=no + touch-action manipulation); запрет выделения/копирования текста и iOS-callout вне input/textarea.
- **Фиксы лагов/скачков (важно — паттерн):**
  - После drop блока моргал скелетон + откат → **оптимистичное** обновление времени локально + `load()` не показывает скелетон после первой загрузки (firstRef). (commit 36be3f3)
  - Создание первой задачи: Empty-плейсхолдер схлопывался в высокую сетку = скачок → **timeline всегда рендерит сетку часов** (пустой день = пустая сетка + хинт, тап по часу создаёт). (commit 6c5bad8)
  - Тап по часу: autoFocus инпута в bottom-sheet утаскивал фон вверх → **scroll-lock фона** при любом оверлее (body position:fixed + restore). (commit dd23334)

## ⭐ СЛЕД. СЕССИЯ (попросил владелец): inline-создание задачи в тапнутой ячейке
Вместо bottom-sheet композера по тапу часа — Apple-стиль: тап/протяжка по пустой сетке → блок «растягивается» на месте в тапнутой ячейке, инлайн-ввод названия там же (не модалка снизу). Спроектировать флоу+мокап, потом сборка. Текущее (scroll-lock) — приемлемо, но владелец хочет именно inline-on-cell.

## ЭКРАН СЕГОДНЯ — отполирован (2026-06-04, 2 деплоя живы)
Живые правки владельца по экрану Сегодня (визуал-гейт в Telegram). Файлы: Today.tsx, DayTimeline.tsx, theme.css, App.tsx.
- **Деплой #1** (commit 7b76742): шапка sticky (`.today-head`); таймлайн с **05:00** (START_HOUR=5, edge: задача <5:00 → сетка авто-растягивается вниз).
- **Деплой #2** (commit ba577ad): Inbox-карта убрана из шапки (Inbox = шторка слева «Входящие»); тап по cal-block → открывает TaskDetail (onOpen проброшен, долг закрыт); чекбокс слева в блоке (`.cal-cb`) — только он done; done-задача остаётся на таймлайне (затемнена).
- **DEPLOY-PATH (ВАЖНО, память врала):** prod-каталог = `/root/planner-v2-src` (НЕ `/root/personal-planner`!). Ветка `planner-v2-phase0`. Деплой = push origin → `ssh root@188.245.42.4` → `cd /root/planner-v2-src && git pull --ff-only` → `cd planner-v2 && docker compose -f docker-compose.prod.yml up -d --build`. Фронт собирается ВНУТРИ образа (dist коммитить НЕ надо). Проверка = `https://planner.188.245.42.4.nip.io/app/` 200 + сверка bundle-хэша. `/root/personal-planner` = ДРУГОЙ репо (planner-bot v1), не путать.
- **Следующий шаг:** финальный визуал-ОК владельца по Сегодня → Календарь-редизайн ИЛИ UX-полировка до Ledger ИЛИ мелочь Волны 2.

## КУРС: паритет с TickTick (ядро сначала)
2026-06-02 проведена РАЗВЕДКА — 5 доменов TickTick разобраны до атомов →
`planner-v2/docs/superpowers/specs/2026-06-02-ticktick-teardown-and-roadmap.md`.
Решения владельца: (1) ядро сначала — волны 2-5; (2) структура папки+списки (TT), миграция с дерева;
(3) Tracking = Habits+Стата+Матрица+Countdown, БЕЗ Pomodoro; (4) внешний календарь DROP, источник
задач = Notion → синхронизация Notion отдельной волной ПОСЛЕ ядра.
Модель уже ~60% (Task/Project/Tag/subtasks/priority/due). Не хватает: CheckItem, мульти-reminder,
структурный recurrence, progress%, Habit/Countdown/Filter/Section + папки.
Решение: `решения/ticktick-parity-2026-06-02.md`.

## Волна 2 СОБРАНА (2026-06-03) — reviewer MERGE, визуал-гейт пройден
Контракт `docs/superpowers/specs/2026-06-02-wave2-contract.md`. Мокапы утверждены (`design/wave2-mock/`).
Собрано Workflow (backend ∥ frontend F1→F2 → reviewer). **Backend: 104 pytest зелёные (+40).**
**Frontend build чистый.** Модель: +CheckItem, +Reminder(мульти), Task.progress/pinned, recurrence_json,
status wont_do; миграция d4e5f6a7b8c9 (round-trip на живом PG ✓). Роуты: GET TaskDetail, PATCH(+complete-recurrence),
checkitems CRUD, PUT reminders. Движок повтора daily/weekly/monthly/yearly base due|completion end never|date|count.
Frontend: TaskDetail(full-screen), Checklist+ring, RecurrenceSheet/RemindersSheet/DateDurationSheet, TaskItem свайпы+multi-select, BatchBar, won't-do, quickParse(NL).
**ВИЗУАЛ-ГЕЙТ пройден живьём** (локальный стек: docker PG :5544 + uvicorn :8000 + vite :5173, initData через fetch-патч):
TaskDetail с реальными данными (кольцо 2/3, чеклист, 2 напоминания), DateDuration-календарь, RecurrenceSheet-стек, список. Скрины `design/wave2-mock/live-*.png`.
CEO-фиксы: формат даты строки списка `3 июн · 17:00` (был ISO+сек), overdue-red, TaskDetail ✓→IcoCheck.
**ХВОСТЫ ЗАКРЫТЫ (2026-06-03, проверено живьём):**
- QuickAddBar примонтирован: FAB→быстрый ввод над таб-баром, NL-подсветка токенов, «Развернуть»→полный composer.
  e2e: «Позвонить юристу завтра 11:00 ~ZIMA !1 #аренда» → задача с due/prio/проект(резолв по имени)/тег(find-or-create). РАБОТАЕТ.
- **CEO поймал+починил баг визуально**: qa-input непрозрачный фон перекрывал highlight-слой → текст в quick-add был невидим. Фон/рамку перенёс на highlight. (build-хэш бы соврал — почему визуал-гейт обязателен.)
- Сабтаски в TaskDetail оживлены: inline-добавление (Enter), toggle, open вложенной (onOpenTask). Секция «Сабтаски N/M». РАБОТАЕТ.
- TaskItem дата строки: `3 июн · 17:00` + overdue-red. TaskDetail ✓→IcoCheck.
**ОСТАЛОСЬ (мелкое, не блок):** composer пишет legacy recurrence:str (полный recurrence-edit есть в TaskDetail-шторке); Tasks.tsx error-state (dead-code, таб не примонтирован); DayTimeline-блок Today onOpen не проброшен; quick-add overlay не закрывается на смене таба.
Скрины live-7/8/9 в `design/wave2-mock/`. Backend 104 теста зелёные (native SQLite-conftest; НЕ ставить DATABASE_URL вручную — собьёт). Локальный стек: docker `planner-pg-w2` :5544 + uvicorn :8000 + vite :5173.
**ЗАДЕПЛОЕНО на prod 2026-06-03** (commit 0eb9ff9, push planner-v2-phase0 → VPS git pull + compose up --build).
planner-migrate прошёл c3d2e1f0a9b8→d4e5f6a7b8c9 на боевом Postgres; таблицы check_item+reminder созданы;
API health ok; публичный https://planner.188.245.42.4.nip.io/app/ → 200. Деплой = git push + VPS `git pull --ff-only` +
`docker compose -f docker-compose.prod.yml up -d --build` (НЕ rsync — у planner-v2 ЕСТЬ github-remote origin, VPS тянет ту же ветку).
Финальный гейт: владелец открывает Mini App в Telegram (@ боте) — визуальное ОК на реальном устройстве.
След.волна по выбору: 3 (Списки: папки/секции/group-sort/фильтры/теги/поиск/kanban) ИЛИ добить мелочь В.2.

## Статус Волны 1: planner-v2 ЖИВОЙ в проде (визуал-проверен 2026-06-02)
Все 5 табов отсняты (`planner-v2/design/wave1-check/`): дизайн по DESIGN.md, IA-каркас целый.
Баг: первый клик по табу иногда теряется (= «мерцание клика» с 05-29, открыт). Списки error-state
«не удалось загрузить проекты» = backend локально не поднят (не баг верстки).

## Статус: planner-v2 ЖИВОЙ в проде

TickTick-клон работает на prod (`planner-188-245-42-4.sslip.io`). Базовый функционал:
категории + drag + контекст-меню + emoji/цвет, календарь-День, полный TaskComposer
(дата/приоритет/проект/теги/подзадачи), ProjectPickerSheet до глубины.

## Текущая работа: IA-rebuild + agent-team build-модель

- Решено перейти на **5 табов**: Сегодня (гибрид) · Календарь · Списки · Цели · Трекинг.
- Принята **agent-team модель** (ростер в `planner-v2/.claude/`, сборка волнами через Workflow).
- Зафиксированы **depth-first процесс** (Фаза1 РАЗВЕДКА → Фаза2 СБОРКА → Фаза3 ГЕЙТ),
  токен-патч и emoji-политика. → `planner-v2/.claude/TEAM.md`, `PATTERNS.md`.

## Сделано
- **Волна 1 «Фундамент-IA» ЗАДЕПЛОЕНА** (8 коммитов, 64 теста, merge).
- Phase-2 depth-gate фиксы: edge-swipe в ListView-over-Lists, ProjectSheet error-state,
  state-matrix (скелетоны + distinct error states tree/ListView).

## Открыто / следующий шаг
- ⚠️ **Волна 1 ждёт визуальной проверки владельцем** (открыть в Telegram/браузере).
- Волны 2-5 — остальные табы (Календарь Неделя/Месяц, Цели, Трекинг) + напоминания/повторы.
- Долг: ProjectSheet async-submit (backlog).

## Не путать
- `planner-bot/` = старая v1, архив. Активное — только `planner-v2/`.
- ~~git remote нет → rsync~~ УСТАРЕЛО. Remote ЕСТЬ, деплой = git pull + compose build. См. блок «DEPLOY-PATH» наверху (prod = `/root/planner-v2-src`).

## ПЕРФ-ФИКС лагов (2026-06-03) — ЗАДЕПЛОЕНО
Жалоба «всё лагучее». Investigate-скилл + chrome perf-trace (4x throttle).
Root cause: app **React-render-bound** (0 React.memo во всём проекте) + **remount экрана на каждый tab-switch**
(Списки ремонтят dnd-kit ×14 useSortable). Layout/Paint дёшевы — узкое место = JS-рендер.
Фиксы (оба live, commits e3e8c91 + e53ae80, бандл index-Djg14S4d):
1. memo(TaskItem/ProjectTree.Row/DayTimeline) + useCallback (App/Today/TaskListBody) → внутри-экранное −37% JS.
2. **keep-alive табов** (App.tsx: mountedTabs + .screen-host[hidden]; экран монтируется раз, дальше show/hide) →
   warm tab-switch FunctionCall 328→73ms (−78%). Cold-load НЕ тяжелее (только Today монтируется на старте).
Полная цепочка baseline→now: 520→73ms JS (−86%) на той же серии переключений.
ПРОВАЛ (откатил): lazy-dnd через rAF — двойной рендер сделал хуже (108→337ms). Урок: не отклад rAF'ом mount dnd.
Остаётся: cold-load render-delay ~1с (мерил на dev — на проде меньше); ListView-колбэки не useCallback (бьют редко).
keep-alive побочка: hidden-экраны рефетчат на bump (больше сети, не лаг).

## СЛЕД. СЕССИЯ — старт с пожеланий владельца (2026-06-03)
1. **UX/UI до уровня Ledger** (приоритет): интерфейс «деревянный/непропорциональный». Подтянуть DESIGN.md
   до точности Ledger (Air Bank вайб), design-audit каждого экрана, чинить пропорции/типографику/ритм. → memory/feedback.md ★.
2. **Календарь** сейчас бесполезен (= копия Today). Редизайн: Неделя/Месяц + time-blocking + undated-tray (Волна 4).
3. **Возобновить бэклог фич**: TaskDetail-ядро готово; открыто Списки-до-атомов (фильтры/секции/папки/поиск/kanban),
   Today-жесты, Habits, Tracking(Stats/Matrix/Countdown), Notion-sync. Roadmap: 2026-06-02-ticktick-teardown-and-roadmap.md.
Галерея сессии: `planner-v2/design/session-2026-06-03.html`.
