# Bug Report — E2E тест 2026-04-26

## BUG-001: Документы не классифицируются
**Симптом:** Файл попал в `learning/files` вместо `work/`.
**Причина:** `document_capture.py` не вызывает Haiku classify → нет `guess_project_slug` → fallback на первый проект в списке.
**Ожидание:** Как в `inbox_capture.py` — classify по caption/filename, показать предложенный проект.
**Workaround:** Нажать "Изменить проект", написать нужный slug.
**Файлы:** `planner_bot/handlers/document_capture.py`

## BUG-002 (потенциальный): voice_capture.py — аналогично
**Предположение:** Голосовые тоже могут не классифицироваться после транскрипции.
**Проверить:** Шаг 4 чеклиста (голосовое).

## Решённые в этой сессии
- NocoDB v2 table IDs вместо имён → `table_map` в клиенте
- SingleSelect options неверный формат → пересоздание таблиц
- `on_process_callback` хардкод `"learning"` → читает из `action_taken`
- `ANTHROPIC_API_KEY` placeholder → реальный ключ
- `docker restart` не перечитывал .env → `docker compose up -d`
